# Analysis: Tainted `null_resource` with `destroy provisioner` does not run the destroy command.

> Tainted null_resource destroy provisioner should execute during re-apply after taint.

**Why it will work now:** Terraform 0.12's improved provisioner lifecycle and taint handling enable this behavior.

---

## Why It Died

The feature relied on Terraform's early provisioner lifecycle which lacked clear semantics for destroy actions after a taint. At the time, the core engine did not differentiate taint‑driven recreation from normal destroy, causing the destroy provisioner to be skipped. Limited test coverage and low priority for null_resource bugs meant the issue never received attention. Community focus shifted to larger provider integrations, leaving this edge case unresolved.

## Why 2026 Changes Everything

Terraform 0.12 introduced a robust provisioner lifecycle model that distinguishes create, read, update, and destroy phases. The later addition of the "lifecycle" block with "replace_triggered_by" and improved taint handling gives precise control over destroy execution. Enhanced testing frameworks and the Go SDK now allow simulation of taint scenarios, making regression verification straightforward. These changes collectively enable reliable implementation of the desired behavior.

## Modern Architecture

Implement a check in the resource's Apply phase that detects a taint flag and forces a destroy‑then‑create cycle for null_resource. Extend the provisioner executor to schedule destroy provisioners when the resource state transitions from tainted to healthy. Introduce a new internal flag in the ResourceDiff struct to indicate taint‑induced replacement. Update the null_resource provider to honor this flag and invoke the existing provisioner runner with a destroy context. Add unit tests using the terraform-plugin-sdk's testing harness to validate that destroy provisioners run on taint‑driven re‑apply. Ensure backward compatibility by gating the new logic behind a version check, defaulting to the legacy path for pre‑0.12 configurations.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~32 hours |
| 🏷️ Tech Tags | terraform |
| 💀 Year Abandoned | 2022 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
