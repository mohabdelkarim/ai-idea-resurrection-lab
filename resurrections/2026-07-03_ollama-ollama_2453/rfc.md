# RFC: Add support for older AMD GPU gfx803, gfx802, gfx805 (e.g. Radeon RX 580, FirePro W7100)

## Summary

This RFC proposes adding support for older AMD GPUs, specifically gfx803, gfx802, and gfx805, to the ollama project. These GPUs are currently not supported due to discontinued official ROCm support. However, recent advancements in the ROCm ecosystem and potential workarounds could make it feasible to support these older GPUs again.

## Motivation

The ollama project currently lacks support for older AMD GPUs like gfx803, gfx802, and gfx805, which include popular models such as the Radeon RX 580 and FirePro W7100. This limitation prevents users with these GPUs from utilizing the ollama project. By adding support for these GPUs, we can expand the project's user base and provide a more inclusive solution for AMD GPU users.

## Detailed Design

To add support for older AMD GPUs, we propose a modular design for the ROCm integration. This design will involve:

1.  Creating a compatibility layer that detects the GPU model and loads the appropriate library.
2.  Modifying the existing ROCm-related code to accommodate these changes.
3.  Utilizing a flexible and backwards-compatible ROCm library and driver.

The compatibility layer will be responsible for:

*   Detecting the GPU model
*   Loading the correct ROCm library based on the GPU model
*   Providing a unified interface for the ollama project to interact with different ROCm libraries

The modular design will allow for easier swaps of different library versions and make it simpler to add support for new GPUs in the future.

## Drawbacks

The main drawbacks of this proposal are:

1.  **Increased Complexity**: Adding support for older GPUs will introduce additional complexity to the ROCm integration, which may lead to increased maintenance costs.
2.  **Performance Overhead**: The compatibility layer and the use of older ROCm libraries might introduce a performance overhead compared to using newer, natively supported GPUs.
3.  **Limited Official Support**: The ROCm libraries and drivers for older GPUs might not be officially supported or maintained, which could lead to issues with stability and security.

## Alternatives

Alternative solutions to adding native support for older AMD GPUs include:

1.  **Cloud-based Services**: Users with older GPUs could utilize cloud-based services that provide access to more modern GPUs, eliminating the need for native support.
2.  **Third-Party Workarounds**: Users could employ third-party workarounds, such as unofficial ROCm libraries or custom drivers, to enable support for older GPUs.

However, these alternatives may not be feasible or desirable for all users, and native support would provide a more seamless and integrated experience.

## Unresolved Questions

The following questions remain unresolved:

1.  **ROCm Library Compatibility**: Will the existing ROCm libraries be compatible with the older GPUs, or will custom libraries be required?
2.  **Performance Impact**: What will be the performance impact of using older ROCm libraries and drivers compared to native support?
3.  **Maintenance and Support**: Who will be responsible for maintaining and supporting the ROCm libraries and drivers for older GPUs?

Addressing these questions will be crucial to the success of this proposal and will require further research and discussion.

---

*RFC generated by Resurrection Bot 🧬*
