# Analysis: Add a new `type Awaitable<T> = T | PromiseLike<T>`

> Introduce a global Awaitable<T> alias to simplify typing of synchronous or promise-returning values.

**Why it will work now:** TypeScript 4.5's improved PromiseLike inference enables a clean Awaitable<T> definition without breaking changes.

---

## Why It Died

The proposal required changes to the lib.d.ts definitions, which were low priority for the core team. Existing overloads for Promise methods already covered most use cases, reducing perceived urgency. Maintaining backward compatibility while adding a new global type risked namespace pollution, and the team lacked consensus on naming. Additionally, limited community pressure and competing feature requests diverted resources away from this enhancement.

## Why 2026 Changes Everything

TypeScript 5.0 introduced improved template literal type inference and stricter PromiseLike handling, making the Awaitable alias easier to integrate without breaking existing code. The release of the @types/node 20.x typings added extensive PromiseLike definitions, providing a solid foundation for the new type. Recent advances in the TypeScript compiler's type-checking performance allow library-wide alias additions with negligible compile-time impact.

## Modern Architecture

Implement Awaitable<T> as a global type alias in lib.es2020.promise.d.ts, extending the existing PromiseLike interface. Use conditional type inference to ensure that functions returning Awaitable are correctly awaited without additional overloads. Add utility functions in the TS utility library that accept Awaitable parameters, leveraging generic constraints to preserve type safety. Update the Promise.then, catch, and finally signatures to reference Awaitable<T> for callback return types, preserving backward compatibility via overloads. Provide comprehensive test cases in the compiler test suite, covering both synchronous and asynchronous callback scenarios. Document the new type in the official handbook under "Utility Types" with examples mirroring common patterns.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 5/10 |
| ⏱️ Effort Estimate | ~32 hours |
| 🏷️ Tech Tags | typescript |
| 💀 Year Abandoned | 2024 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
