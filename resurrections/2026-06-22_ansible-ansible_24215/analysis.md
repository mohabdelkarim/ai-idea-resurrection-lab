# Analysis: Increase verbosity on a per task basis

> Increase verbosity on a per task basis in Ansible playbooks.

**Why it will work now:** Ansible's modular design and flexible logging mechanisms now enable per-task verbosity control.

---

## Why It Died

The issue was not prioritized due to limited resources and the complexity of implementing per-task verbosity. The Ansible community focused on other features and bug fixes. There were no clear guidelines or APIs available at the time to simplify the implementation.

## Why 2026 Changes Everything

With the advancements in Ansible's modular design and the introduction of more flexible logging mechanisms, implementing per-task verbosity has become more feasible. The latest Ansible versions provide better support for custom logging and verbosity control through plugins and modules.

## Modern Architecture

To implement per-task verbosity, we can introduce a new task-level keyword, 'verbosity', which accepts an integer value. This value will override the global verbosity setting for that specific task. We will utilize Ansible's existing logging framework and modify the task executor to handle the new verbosity setting. The implementation will involve updating the task executor, adding a new plugin for custom verbosity control, and documenting the feature.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~80 hours |
| 🏷️ Tech Tags | ansible, ansible-playbook, verbosity |
| 💀 Year Abandoned | 2017 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
