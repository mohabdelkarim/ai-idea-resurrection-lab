# Analysis: Finding by owner username/group/uid/gid

> Add a feature to fd to search files by owner username, group, uid, or gid.

**Why it will work now:** Rust's users and gid crates now provide a straightforward way to implement this feature.

---

## Why It Died

The issue was abandoned likely due to the lack of immediate demand and potential complexity of implementing the feature. The discussion didn't gain enough traction, and no one took on the task of implementing it. Additionally, the author didn't provide a ready-to-use patch, which might have accelerated the process.

## Why 2026 Changes Everything

With the evolution of Rust's ecosystem, particularly with the stabilization of the `users` and `gid` crates, implementing this feature has become more feasible. Modern Rust offers better support for working with users, groups, and file systems, making the task less daunting. The `fd` tool itself has also seen significant updates, potentially simplifying the integration of this feature.

## Modern Architecture

The feature could be implemented by adding a new flag, `--owner`, which accepts a string in the format of `[(uid|username)][:(gid|groupname)]`. This would involve using the `users` and `gid` crates to parse and validate the input. The implementation would require modifying the `fd` core to handle the new flag and integrate it with the existing file search logic. The `--owner` flag would be mutually exclusive with other flags that affect file filtering.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~80 hours |
| 🏷️ Tech Tags | fd, rust, filesystem |
| 💀 Year Abandoned | 2020 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
