# Analysis: wrapping by word would be awesome

> Add a word‑aware wrapping mode to bat to prevent splitting words across lines.

**Why it will work now:** Rust's stable Unicode‑segmentation crate 1.10 enables accurate word boundaries for text.

---

## Why It Died

The original request required reliable word‑boundary detection, which bat lacked without a stable Unicode segmentation library. Maintaining the codebase was already a part‑time effort, leaving little bandwidth for non‑critical features. Additionally, the existing wrap implementation was tightly coupled to byte‑wise logic, making a word‑aware mode risky to add without a refactor.

## Why 2026 Changes Everything

The Unicode‑segmentation crate reached version 1.10 with a stable API for iterating over Unicode words, removing previous reliability concerns. clap v4 now supports enum arguments, simplifying the addition of a new wrap mode flag. The Rust ecosystem’s emphasis on zero‑cost abstractions makes integrating word‑wise wrapping efficient and maintainable.

## Modern Architecture

Introduce a new enum WrapMode { Char, Word } exposed via the --wrap flag using clap's enum support. Extend the rendering pipeline to accept a WrapMode parameter and branch to either the existing byte‑wise wrap or a new word‑wise path. The word‑wise path will use unicode_segmentation::UnicodeSegmentation::unicode_words to split lines into word slices, then reassemble them respecting the terminal width. Update the configuration struct to store the selected mode and ensure backward compatibility by defaulting to Char mode. Add comprehensive unit tests covering ASCII, multibyte, and emoji text, and update the man page and --help output accordingly.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 5/10 |
| ⏱️ Effort Estimate | ~32 hours |
| 🏷️ Tech Tags | rust, cli |
| 💀 Year Abandoned | 2023 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
