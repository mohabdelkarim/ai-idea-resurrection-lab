# RFC: [Feature Request] Zoxide Library

## Summary

This RFC proposes the creation of a zoxide library to integrate zoxide's functionality into other Rust tools and file managers. The library will provide a simple and intuitive API for querying and interacting with the zoxide database, allowing developers to build custom tools and integrations.

## Motivation

The zoxide tool has gained popularity as a fast and efficient way to navigate directories. However, there is a growing demand to integrate zoxide's functionality into other tools and file managers. The existence of libraries like skim and the growing ecosystem of Rust tools, including file managers like joshuto, provide a clear use case for a zoxide library. By creating a library, we can enable developers to build custom tools and integrations that leverage zoxide's capabilities.

## Detailed Design

The zoxide library will be designed as a Rust crate, providing a simple and intuitive API for integrating zoxide's functionality into other tools. The library will use a builder pattern to configure the zoxide instance, and provide methods for querying and interacting with the database. The library will also use async/await to provide a non-blocking interface.

The library will have the following features:

* A `Zoxide` class that provides a builder pattern for configuring the zoxide instance
* A `Database` class that provides methods for querying and interacting with the database
* A `Query` class that provides methods for querying the database

The library will use popular Rust libraries like `anyhow` for error handling and `tokio` for async support.

## Drawbacks

The main drawback of creating a zoxide library is the added complexity and maintenance burden on the zoxide project. The library will require additional testing, documentation, and maintenance to ensure that it is stable and reliable.

## Alternatives

One alternative to creating a zoxide library is to provide a CLI interface for zoxide and rely on external tools to integrate with zoxide. However, this approach has limitations, as it requires developers to parse the CLI output and implement their own integrations. By providing a library, we can enable developers to build more seamless and integrated tools.

## Unresolved Questions

The following questions remain unresolved:

* What is the optimal API design for the zoxide library?
* How will the library handle errors and edge cases?
* What is the best approach for testing and validating the library?
* How will the library be documented and maintained?

These questions will need to be addressed as the library is designed and implemented.

---

*RFC generated by Resurrection Bot 🧬*
