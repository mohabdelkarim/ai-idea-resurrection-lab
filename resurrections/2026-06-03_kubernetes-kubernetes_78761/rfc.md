# RFC: HPA doesn't scale down to minReplicas even though metric is under target

# Summary

The Horizontal Pod Autoscaler (HPA) in Kubernetes fails to scale down to the minimum number of replicas (minReplicas) even when the metric being monitored is under the target value. This results in inefficient resource usage and unnecessary costs. This RFC proposes a solution to modify the HPA's scaling logic to ensure it scales down to minReplicas when the metric is under target.




# Motivation

The current behavior of HPA can lead to over-provisioning of resources, resulting in wasted resources and increased costs. In scenarios where the workload is low, and the metric is under the target, it is expected that HPA should scale down to the minimum number of replicas. However, due to the current implementation, HPA may not scale down to minReplicas, leading to inefficient resource utilization.




# Detailed Design

The proposed solution involves updating the HPA's scaling logic to consider minReplicas when scaling down. The changes include:

1.  **Modified Scaling Algorithm**: Update the HPA's scaling algorithm to take into account the minReplicas value when determining the desired number of replicas. When the metric is under target, the algorithm should scale down to minReplicas.

2.  **Integration with Metrics API**: Leverage the Metrics API to fetch accurate metrics and make informed scaling decisions.

3.  **Edge Case Handling**: Enhance the HPA's logic to handle edge cases, such as sudden changes in workload or metrics, to ensure smooth scaling.




# Drawbacks

The proposed changes may have the following drawbacks:

1.  **Increased Complexity**: The updated scaling logic may introduce additional complexity, potentially affecting the performance and reliability of the HPA.

2.  **Potential for Over-Scaling**: If not tuned properly, the updated algorithm may lead to over-scaling, resulting in increased costs.




# Alternatives

Alternative solutions considered include:

1.  **Using Custom Scaling Solutions**: Implementing custom scaling solutions using external metrics and scaling algorithms.

2.  **Modifying Existing Scaling Solutions**: Updating existing scaling solutions, such as the Cluster Autoscaler, to handle edge cases.

However, these alternatives may require significant development and maintenance efforts, and may not provide a straightforward solution to the problem.




# Unresolved Questions

The following questions remain unresolved:

1.  **Tuning Parameters**: What are the optimal tuning parameters for the updated scaling algorithm to ensure efficient scaling?

2.  **Edge Case Handling**: How can the HPA's logic be enhanced to handle edge cases and sudden changes in workload or metrics?

3.  **Performance Impact**: What is the potential performance impact of the updated scaling logic on the HPA and the cluster?

These questions will be addressed through further research, testing, and iteration on the proposed solution.

---

*RFC generated by Resurrection Bot 🧬*
