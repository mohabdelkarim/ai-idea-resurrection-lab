# Analysis: `terraform show <plan>` "Backend reinitialization required" within subfolder

> Allow `terraform show` to work in subfolders without requiring backend reinitialization.

**Why it will work now:** The new Terraform 1.6+ workspace management and improved plan file handling make this feasible now.

---

## Why It Died

The issue was abandoned due to the complexity of reinitializing the backend when running `terraform show` in a subfolder. The error message 'Backend reinitialization required' indicated that the backend needed to be reinitialized, but this was not feasible with the current implementation.

## Why 2026 Changes Everything

With the introduction of Terraform 1.5 and the new `terraform workspace` command, it is now possible to manage multiple workspaces and backends more efficiently. Additionally, the `terraform show` command has been improved to handle plan files more robustly.

## Modern Architecture

To implement this feature, we can modify the `terraform show` command to detect when it is being run in a subfolder and handle the backend initialization accordingly. We can use the `terraform` package's `Init` function to initialize the backend and load the plan file. The `terraform` package's `Show` function can then be used to display the plan.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | terraform, go, backend |
| 💀 Year Abandoned | 2019 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | No |

---

*Analysis generated by Resurrection Bot 🧬*
