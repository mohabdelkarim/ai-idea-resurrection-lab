# Analysis: Allow specifying JSON stringifier

> Allow custom JSON stringifier for res.json() in Express.js.

**Why it will work now:** The addition of json-bigint and similar libraries now makes this feasible.

---

## Why It Died

The issue was abandoned likely due to the lack of a clear use case and potential performance implications. The Express.js team may have been hesitant to add a new feature that could affect the behavior of the res.json() method. Additionally, the team may have been focused on other priorities at the time.

## Why 2026 Changes Everything

With the growing adoption of JSON stringifiers like json-bigint and the increasing importance of handling large integers in JSON data, this feature has become more relevant. Modern tooling and libraries have made it easier to implement custom stringifiers, reducing the potential effort and complexity. The recent updates to JSON stringification libraries have also improved performance.

## Modern Architecture

To implement this feature, we can add an optional 'stringifier' option to the res.json() method. This option would allow users to pass a custom stringifier function, such as json-bigint.stringify. We can also provide a default stringifier that falls back to JSON.stringify. The implementation would involve updating the res.json() method to accept the new option and using it to stringify the response data.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~24 hours |
| 🏷️ Tech Tags | express, json, typescript |
| 💀 Year Abandoned | 2020 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
