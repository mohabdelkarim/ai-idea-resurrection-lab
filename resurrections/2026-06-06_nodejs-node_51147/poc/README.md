# Proof of Concept: `--env-file` support in `NODE_OPTIONS`

**Language:** typescript
**Estimated run time:** < 5 minutes

## Prerequisites

- Node.js 20+, npm install

## How to Run

```bash
npm install
npx ts-node main.ts
```

## What This Demonstrates

Support --env-file in NODE_OPTIONS for centralized env file management.
