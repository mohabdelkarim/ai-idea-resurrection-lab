# RFC: Ability to accept input from Charts, Tables, etc.

### Summary

This RFC proposes the introduction of a new feature in Streamlit that enables users to interact with output elements such as charts and tables to send input back to the Python script. This feature will enhance the interactivity and usability of Streamlit applications, allowing for more dynamic and responsive user experiences.

The proposed solution involves creating a new module for handling interactive elements, utilizing Python's asyncio for asynchronous updates, and leveraging Streamlit's existing components for charts and dataframes. A generic event handler will be implemented to capture and relay user interactions to the Python script.

### Motivation

Streamlit's current architecture primarily supports one-way interactions, where users can input data through widgets, but output elements like charts and tables are not interactive. This limitation hinders the creation of more engaging and responsive applications.

Recent advancements in Streamlit's architecture, such as the introduction of more modular and extensible components, make this feature more feasible. The growing adoption of Python's asynchronous programming and the improvement in browser-based technologies also play a crucial role. Specifically, the use of WebAssembly (WASM) and modern JavaScript frameworks could simplify the implementation of interactive elements.

### Detailed Design

The implementation will involve the following steps:

1. **New Module for Interactive Elements**: Create a new module in Streamlit for handling interactive elements. This module will provide a base class for interactive elements and define the API for event handling and data exchange between the frontend and backend.

2. **Asynchronous Updates**: Utilize Python's asyncio library to enable asynchronous updates of interactive elements. This will allow the Python script to handle user interactions without blocking the main thread.

3. **Leverage Existing Components**: Leverage Streamlit's existing components for charts and dataframes to display interactive elements. This will reduce the development effort and ensure consistency with the existing API.

4. **Generic Event Handler**: Implement a generic event handler that can capture and relay user interactions to the Python script. This event handler will be responsible for handling events such as clicks, hovers, and selections.

5. **API for Event Handling and Data Exchange**: Define a clear API for event handling and data exchange between the frontend and backend. This API will enable the Python script to receive user interactions and update the application state accordingly.

### Drawbacks

The proposed solution has the following drawbacks:

1. **Technical Complexity**: Implementing interactive elements will require significant technical effort, especially in handling asynchronous updates and event handling.

2. **Performance Impact**: The introduction of interactive elements may impact the performance of Streamlit applications, especially if not optimized properly.

3. **Additional Maintenance**: The new feature will require additional maintenance effort to ensure that it remains stable and secure.

### Alternatives

The following alternatives were considered:

1. **Third-Party Libraries**: Utilize third-party libraries that provide interactive elements for charts and tables. However, this approach may lead to inconsistencies with the existing API and require additional maintenance effort.

2. **Custom Solutions**: Implement custom solutions for each use case that requires interactive elements. However, this approach may lead to duplicated effort and inconsistencies across different applications.

### Unresolved Questions

The following questions remain unresolved:

1. **Scalability**: How will the new feature impact the scalability of Streamlit applications?

2. **Security**: How will the new feature affect the security of Streamlit applications?

3. **User Adoption**: How will users adopt the new feature, and what are the potential use cases?

These questions will need to be addressed as the implementation progresses.

---

*RFC generated by Resurrection Bot 🧬*
