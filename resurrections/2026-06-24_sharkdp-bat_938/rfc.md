# RFC: Ubuntu install error when rg is installed also

## Summary

The installation of `bat` and `ripgrep` on Ubuntu 20.04 conflicts due to overlapping files, specifically the `/usr/.crates2.json` file. This RFC proposes a solution to resolve this conflict by modifying the Debian packaging scripts for `bat` and `ripgrep` to use a more coordinated approach for handling installation.

## Motivation

The conflict between `bat` and `ripgrep` packages during installation on Ubuntu 20.04 was caused by both packages trying to install files to the same location, `/usr/.crates2.json`. This issue arose due to the way the packages were configured and the lack of proper coordination between the maintainers of `bat` and `ripgrep`. The Rust ecosystem has evolved significantly since 2020, with improvements in Cargo, Rust's package manager, and the Debian packaging process for Rust applications. These improvements can be leveraged to resolve such conflicts.

## Detailed Design

To resolve this issue, we propose the following:

1. Modify the Debian packaging scripts for `bat` and `ripgrep` to use a more coordinated approach for handling installation.
2. Use Cargo's features to manage dependencies and ensure that files are installed to the correct locations.
3. Utilize the `dh-rust` tool to simplify the packaging process and reduce conflicts.
4. Implement a more robust conflict detection and resolution mechanism during package installation.

Specifically, we can use the `dh-rust` tool to:

* Manage the installation of files to the correct locations.
* Handle conflicts between packages during installation.
* Provide a more robust and coordinated approach for handling package installation.

## Drawbacks

The proposed solution requires modifications to the Debian packaging scripts for `bat` and `ripgrep`, which may introduce additional complexity and maintenance overhead. Additionally, the use of `dh-rust` tool may require additional dependencies and setup.

## Alternatives

Alternative solutions include:

1. Renaming the conflicting files in one of the packages.
2. Using a different package manager or installation approach.
3. Implementing a more complex conflict detection and resolution mechanism.

However, these alternatives may introduce additional complexity, maintenance overhead, or limitations.

## Unresolved Questions

1. What are the specific requirements for the Debian packaging scripts for `bat` and `ripgrep` to use the `dh-rust` tool?
2. How will the conflict detection and resolution mechanism be implemented and tested?
3. What are the potential risks and limitations of using the `dh-rust` tool and the proposed solution?

These questions will be addressed through further research, testing, and iteration on the proposed solution.

---

*RFC generated by Resurrection Bot 🧬*
