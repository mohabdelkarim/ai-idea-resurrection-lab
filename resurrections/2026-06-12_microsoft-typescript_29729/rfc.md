# RFC: Literal String Union Autocomplete

## 1. Summary

This RFC proposes to enhance TypeScript's autocompletion capabilities for literal string unions with string types. The goal is to provide more accurate and helpful autocompletion suggestions when working with unions of string literals and the general string type.

## 2. Motivation

TypeScript's current autocompletion support for literal string unions is limited. When a developer is working with a union type that includes string literals and the general string type, the autocompletion suggestions are often incomplete or inaccurate. This can lead to a poor developer experience, especially in popular frameworks and libraries that heavily utilize string unions.

Recent advancements in TypeScript's type inference and IntelliSense capabilities have made it possible to revisit this feature. The introduction of more efficient algorithms for type checking and autocompletion has increased the priority of implementing better autocompletion support for literal string unions.

## 3. Detailed Design

To implement this feature, we will leverage TypeScript's existing type inference capabilities and enhance the IntelliSense algorithm to handle unions of string literals and string types more effectively. The proposed design involves the following steps:

*   Introduce a new type checker that can distinguish between string literal types and the general string type.
*   Update the IntelliSense engine to support this new type checker and provide more accurate autocompletion suggestions.
*   Modify the TypeScript compiler's API to accommodate the new type checker and IntelliSense engine.

The new type checker will be able to analyze the union type and provide a list of possible string literal values, as well as the general string type. The IntelliSense engine will then use this information to provide more accurate autocompletion suggestions.

## 4. Drawbacks

The proposed design has some potential drawbacks:

*   **Performance implications**: The introduction of a new type checker and updated IntelliSense engine may have performance implications, particularly for large and complex codebases.
*   **Edge cases**: There may be edge cases where the new type checker and IntelliSense engine do not behave as expected, such as when working with complex union types or nested generics.

## 5. Alternatives

Alternative solutions were considered, including:

*   **Improving the existing type checker**: One possible alternative is to improve the existing type checker to handle unions of string literals and string types more effectively. However, this approach may not provide the same level of accuracy and completeness as the proposed design.
*   **Using a third-party library**: Another possible alternative is to use a third-party library to provide autocompletion support for literal string unions. However, this approach may not be as seamless or integrated as the proposed design.

## 6. Unresolved Questions

The following questions remain unresolved:

*   **How will the new type checker handle edge cases, such as complex union types or nested generics?**
*   **What are the performance implications of the proposed design, and how can they be mitigated?**
*   **How will the proposed design interact with existing TypeScript features, such as conditional types and type guards?**

These questions will need to be addressed as the proposed design is implemented and tested.

---

*RFC generated by Resurrection Bot 🧬*
