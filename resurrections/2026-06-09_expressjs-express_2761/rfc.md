# RFC: Support for module http2?

## 1. Summary

This RFC proposes the addition of native support for the http2 protocol in Express.js. By leveraging the built-in http2 module in Node.js, Express will be able to handle http2 requests and responses, providing a seamless experience for developers and users.

## 2. Motivation

The http2 protocol has gained significant traction in recent years due to its performance benefits over the traditional http1.1 protocol. With the maturation of the http2 module and its incorporation into Node.js core since Node.js 8.4.0, it has become feasible to integrate http2 support into Express.js. This integration will enable Express applications to take advantage of http2 features such as multiplexing, server push, and improved header compression.

## 3. Detailed Design

To implement http2 support in Express, we will create an http2 adapter that ensures compatibility with existing middleware and routes. The adapter will handle http2 streams and introduce new APIs for handling http2-specific features like server push. The design will involve the following steps:

*   Update the Express core to handle http2 streams
*   Introduce new APIs for handling http2-specific features like server push
*   Ensure compatibility with existing middleware and routes
*   Utilize TypeScript to maintain type safety and compatibility across different Node.js versions

## 4. Drawbacks

The main drawbacks of this proposal are:

*   **Complexity**: Implementing http2 support will add complexity to the Express codebase, which may lead to increased maintenance costs.
*   **Backwards Compatibility**: Supporting http2 may require changes to existing APIs or introduce new ones, which could affect backwards compatibility.
*   **Performance Overhead**: Handling http2 streams and features may introduce a performance overhead compared to traditional http1.1 requests.

## 5. Alternatives

The alternatives to this proposal are:

*   **Use an external http2 library**: Instead of integrating http2 support directly into Express, we could rely on an external library like node-http2. However, this approach may lead to compatibility issues and additional maintenance costs.
*   **Wait for further http2 adoption**: We could wait for further adoption of http2 in the Node.js ecosystem and Express community. However, this approach may delay the benefits of http2 support for Express users.

## 6. Unresolved Questions

The following questions remain unresolved:

*   **How will we handle http2-specific features like server push?**: We need to discuss how to design and implement APIs for handling http2-specific features like server push.
*   **What are the performance implications of http2 support?**: We need to investigate the performance implications of http2 support and identify potential bottlenecks.
*   **How will we ensure compatibility with existing middleware and routes?**: We need to discuss how to ensure compatibility with existing middleware and routes and identify potential issues.

---

*RFC generated by Resurrection Bot 🧬*
