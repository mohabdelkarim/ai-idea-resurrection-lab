# Analysis: Run job on each node once to help with setup

> Run a job on each Kubernetes node once for setup and initialization purposes.

**Why it will work now:** Kubeadm 1.22+ and OPA Gatekeeper 2.0 make node-specific initialization safer and more feasible.

---

## Why It Died

The issue was likely abandoned due to the lack of a clear use case and potential security concerns. Running a job on each node in the cluster once can be useful for setup, but it also introduces risks if not properly validated. In 2018, Kubernetes was still maturing, and the focus was on core features rather than node-specific initialization.

## Why 2026 Changes Everything

With the maturity of Kubernetes and the introduction of new features like Kubeadm, the use case for running a job on each node once has become more relevant. Additionally, advancements in security and access control, such as PSPs and OPA Gatekeeper, have made it safer to execute node-specific jobs. The growing adoption of Kubernetes in large-scale environments also increases the demand for such a feature.

## Modern Architecture

The feature could be implemented using a DaemonSet with a specific annotation or label that ensures it runs only once per node. The job could be triggered by a custom controller that watches for node creation events. To ensure security, the feature could utilize Kubernetes' built-in RBAC and PSPs to restrict the job's privileges. The implementation would involve creating a new controller and integrating it with the existing Kubernetes components.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~80 hours |
| 🏷️ Tech Tags | kubernetes, daemonset, kubeadm |
| 💀 Year Abandoned | 2018 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
