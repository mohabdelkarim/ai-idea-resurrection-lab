# RFC: Run job on each node once to help with setup

# 1. Summary

This RFC proposes a feature to run a job on each Kubernetes node once for setup and initialization purposes. The job can be used to perform node-specific tasks such as setting up node-specific configuration, installing node-specific software, or running node-specific tests.

The proposed solution involves creating a DaemonSet with a specific annotation or label that ensures it runs only once per node. A custom controller will be created to watch for node creation events and trigger the job.

## 2. Motivation

Running a job on each node in the cluster once can be useful for a variety of use cases, including:

* Setting up node-specific configuration, such as setting environment variables or creating node-specific directories.
* Installing node-specific software, such as device drivers or node-specific libraries.
* Running node-specific tests, such as network or storage tests.

Currently, there is no built-in way to run a job on each node once, which makes it difficult to perform these tasks. This feature will make it easier for users to manage their nodes and perform node-specific tasks.

## 3. Detailed Design

The proposed solution involves the following components:

* A DaemonSet with a specific annotation or label that ensures it runs only once per node.
* A custom controller that watches for node creation events and triggers the job.
* A Job template that defines the job to be run on each node.

The custom controller will be responsible for creating the Job on each node. The Job will be created with a specific label or annotation that ensures it runs only once per node.

The DaemonSet will be used to ensure that the Job runs on each node. The DaemonSet will be created with a specific label or annotation that ensures it runs only once per node.

The Job template will define the job to be run on each node. The Job template will include the command to be run, the environment variables to be set, and any other configuration required for the job.

## 4. Drawbacks

The proposed solution has the following drawbacks:

* Security risks: Running a job on each node in the cluster can introduce security risks if not properly validated. The job could potentially access sensitive information or modify node-specific configuration.
* Complexity: The proposed solution requires creating a custom controller and integrating it with the existing Kubernetes components. This can add complexity to the system and make it harder to maintain.

## 5. Alternatives

The following alternatives have been considered:

* Using a DaemonSet to run a job on each node. However, this approach does not ensure that the job runs only once per node.
* Using a Job to run a job on each node. However, this approach requires creating a separate Job for each node, which can be complex and difficult to manage.

## 6. Unresolved Questions

The following questions remain unresolved:

* How to ensure that the job runs only once per node?
* How to handle node failures or deletions?
* How to integrate the custom controller with the existing Kubernetes components?

These questions will need to be addressed before the feature can be implemented.

---

*RFC generated by Resurrection Bot 🧬*
