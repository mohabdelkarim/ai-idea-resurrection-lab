# Analysis: invoke a pager to display results

> Automatically invoke a pager to display results in ripgrep when output is not piped.

**Why it will work now:** The feature is now feasible due to improved terminal libraries and pager handling in Rust, making it a central and.

---

## Why It Died

The issue was labeled as 'wontfix' because the author of ripgrep did not see the value in automatically invoking a pager. At the time, there were concerns about platform compatibility and pager behavior. Additionally, users could already pipe the output of ripgrep to a pager themselves.

## Why 2026 Changes Everything

With the existence of modern terminal libraries like crossterm and console, it's easier to handle platform-specific pager invocation. The Rust ecosystem also provides libraries like terminal-pager that simplify pager handling. These tools make it feasible to implement a pager invocation feature that works across different platforms.

## Modern Architecture

The feature could be implemented using the Command pattern, where a PagerInvoker class handles detecting whether the output is piped and invoking the pager. The design would involve using a library like terminal-pager to handle pager invocation. A configuration option could be added to allow users to opt-out of automatic pager invocation. The implementation would likely involve modifying the main entry point of ripgrep to use the PagerInvoker.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | terminal, pager, rust |
| 💀 Year Abandoned | 2016 |
| 🔬 Has PoC | No |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
