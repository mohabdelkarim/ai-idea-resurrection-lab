# RFC: invoke a pager to display results

## Summary

This RFC proposes the addition of a feature to automatically invoke a pager to display results in ripgrep when the output is not piped to another command or file. The goal is to improve the user experience by providing a more convenient way to browse through search results. This feature will be implemented using a library like terminal-pager to handle pager invocation.

## Motivation

The motivation behind this feature is to enhance the usability of ripgrep, especially for users who are not familiar with piping output to a pager. Currently, users have to manually pipe the output of ripgrep to a pager, such as less or more, to browse through the results. By automatically invoking a pager when output is not piped, ripgrep can provide a more user-friendly experience. Additionally, with the advancements in modern terminal libraries, it is now more feasible to implement this feature in a platform-agnostic way.

## Detailed Design

The design of this feature will involve using the Command pattern, where a PagerInvoker class will handle detecting whether the output is piped and invoking the pager. The implementation will utilize a library like terminal-pager to handle pager invocation. The PagerInvoker class will be responsible for checking if the output is a tty and if a pager is available. If both conditions are met, it will invoke the pager with the output of ripgrep. A configuration option will be added to allow users to opt-out of automatic pager invocation.

## Drawbacks

One of the drawbacks of this feature is the potential for platform-specific issues with pager invocation. Although modern terminal libraries have made it easier to handle these issues, there may still be edge cases that need to be addressed. Another drawback is the added complexity to the codebase, which may introduce new bugs. However, the benefits of improved usability and convenience are expected to outweigh these drawbacks.

## Alternatives

An alternative to automatically invoking a pager is to provide an option for users to manually specify a pager command. However, this approach would still require users to know how to use a pager, which may not be desirable for all users. Another alternative is to rely on the operating system's default behavior for handling output, but this may not provide the best user experience. The proposed solution of automatically invoking a pager provides a more streamlined and user-friendly experience.

## Unresolved Questions

One unresolved question is how to handle the case where a user has specified a pager command in their environment, but it is not available. In this case, ripgrep could fall back to a default pager or display an error message. Another unresolved question is how to handle the case where the output of ripgrep is piped to another command, but the user has specified a pager command. In this case, ripgrep could ignore the pager command and pipe the output to the specified command. These questions will need to be addressed during the implementation of the feature.

---

*RFC generated by Resurrection Bot 🧬*
