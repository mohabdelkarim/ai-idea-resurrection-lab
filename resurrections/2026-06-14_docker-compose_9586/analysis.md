# Analysis: "Custom build outputs" support

> Docker Compose should support custom build outputs via a new configuration option.

**Why it will work now:** Docker BuildKit's API now enables custom build output implementations.

---

## Why It Died

The feature request for custom build outputs in Docker Compose was likely abandoned due to the lack of a clear use case and prioritization. At the time, Docker Compose was focused on stabilizing its core functionality. The custom build outputs feature required significant changes to the build process and may have been deemed too complex or niche.

## Why 2026 Changes Everything

With the advancements in Docker and containerization technologies, supporting custom build outputs has become more feasible. The introduction of new Docker APIs and improvements in Docker Compose's extensibility make it possible to revisit this feature. Specifically, the Docker BuildKit and its API provide a foundation for implementing custom build outputs.

## Modern Architecture

To implement custom build outputs in Docker Compose, we can leverage Docker BuildKit's API to extend the build process. We will introduce a new configuration option in the Compose file to specify custom build outputs. The implementation will involve creating a new module in Docker Compose to handle BuildKit API interactions and integrate it with the existing build process. This will enable users to define custom outputs for their builds, such as exporting images to multiple registries or generating additional artifacts.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~80 hours |
| 🏷️ Tech Tags | docker, compose, buildkit |
| 💀 Year Abandoned | 2022 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
