# RFC: Please expose current user and group ID as new variables for use inside docker-compose.yml

Summary

--------

Expose the current user and group ID as new variables, `COMPOSE_USER_ID` and `COMPOSE_GROUP_ID`, for use inside `docker-compose.yml` to simplify the specification of the `user` parameter and improve the overall user experience.



Motivation

-----------

The current process for specifying the `user` parameter in `docker-compose.yml` requires manually determining and entering the user ID and group ID of the user running the `docker-compose` command. This can be cumbersome and prone to errors. By exposing the current user and group ID as environment variables, users can easily reference them in their `docker-compose.yml` files, making it easier to run containers with the correct user and group permissions.



Detailed Design

----------------

To implement this feature, we will introduce two new environment variables, `COMPOSE_USER_ID` and `COMPOSE_GROUP_ID`, which will be populated with the effective user ID and group ID of the user running the `docker-compose` command. These variables will be made available to the `docker-compose.yml` file, allowing users to reference them when specifying the `user` parameter.

The variables will be implemented using the `os/user` and `os/group` packages in Go, which provide functions for getting the current user and group IDs. The variables will be added to the `docker-compose` command's environment and will be accessible to all services defined in the `docker-compose.yml` file.

Example usage:
yaml
version: '3'
services:
  myservice:
    image: myimage
    user: ${COMPOSE_USER_ID}:${COMPOSE_GROUP_ID}


Drawbacks

----------

One potential drawback of this feature is that it may introduce security concerns if not implemented properly. For example, if the `COMPOSE_USER_ID` and `COMPOSE_GROUP_ID` variables are not properly validated, they could potentially be used to exploit vulnerabilities in the system. However, by using the `os/user` and `os/group` packages and following best practices for environment variable handling, we can mitigate these risks.

Another potential drawback is that this feature may not be compatible with all use cases, such as running `docker-compose` in a non-standard environment or with a specific user and group configuration. However, by providing a standardized solution that follows the principles of the 12-factor app, we can ensure that this feature is compatible with a wide range of use cases.



Alternatives

------------

One alternative to this feature would be to require users to define the `user` parameter manually, without providing any environment variables. However, this approach would be less convenient and more prone to errors.

Another alternative would be to provide a separate configuration file or command-line option for specifying the user ID and group ID. However, this approach would add complexity and would not provide the same level of convenience and flexibility as exposing environment variables.



Unresolved Questions

--------------------

One unresolved question is how to handle cases where the user running the `docker-compose` command does not have a valid user ID or group ID. For example, if the user is running the command in a non-standard environment or with a specific user and group configuration, the `COMPOSE_USER_ID` and `COMPOSE_GROUP_ID` variables may not be populated correctly.

Another unresolved question is how to handle cases where the `docker-compose.yml` file is being used in a non-standard way, such as being used to generate a Docker image or being used with a custom `docker` command. In these cases, the `COMPOSE_USER_ID` and `COMPOSE_GROUP_ID` variables may not be relevant or may need to be handled differently.

To address these questions, we will need to conduct further research and testing to ensure that this feature is implemented correctly and is compatible with a wide range of use cases.

---

*RFC generated by Resurrection Bot 🧬*
