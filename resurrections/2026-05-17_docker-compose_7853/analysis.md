# Analysis: Please expose current user and group ID as new variables for use inside docker-compose.yml

> Expose current user and group ID as new variables for use inside docker-compose.yml.

**Why it will work now:** This feature is necessary to allow users to run containers with the same user and group ID as the docker-compose.

---

## Why It Died

The feature request did not gain enough traction and was not prioritized by the maintainers. The issue was also related to security concerns and the need for a standardized solution. Additionally, there were alternative workarounds available, such as defining the variables outside of the docker-compose.yml file.

## Why 2026 Changes Everything

With the advancements in Docker and Compose, the security concerns have been addressed, and the need for a standardized solution has become more pressing. The recent updates to the Docker and Compose APIs, such as the introduction of new environment variables and improved support for user and group IDs, have made it possible to implement this feature. Specifically, the use of BuildKit and the new `docker build` command have improved the handling of user and group IDs.

## Modern Architecture

The modern design for this feature would involve introducing two new environment variables, `COMPOSE_USER_ID` and `COMPOSE_GROUP_ID`, which would be populated with the effective user ID and group ID of the user running the `docker-compose` command. This would be achieved through the use of the `os/user` and `os/group` packages in Go, which provide functions for getting the current user and group IDs. The variables would be made available to the `docker-compose.yml` file, allowing users to specify the `user` parameter in a more convenient and standardized way. The design would follow the principles of the 12-factor app and would be consistent with the existing environment variable-based configuration in Docker and Compose.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~48 hours |
| 🏷️ Tech Tags | docker, compose, go, environment variables |
| 💀 Year Abandoned | 2020 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
