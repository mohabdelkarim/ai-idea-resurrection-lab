# RFC: Using generateStaticParams with next/headers causes dynamicParams ignored in production

## Summary

This RFC proposes a solution to the issue of Next.js ignoring dynamicParams in production when using generateStaticParams with next/headers. The goal is to enhance the compatibility of static site generation with dynamic server-side features.

## Motivation

The current behavior of Next.js, where dynamicParams are ignored in production when using generateStaticParams with next/headers, leads to unexpected behavior and limits the flexibility of server-side rendering. This issue arises from the fundamental conflict between static site generation and dynamic server-side features. Recent advancements in Next.js, particularly with the release of version 14 and improvements in server components, provide an opportunity to address this issue.

## Detailed Design

The proposed solution involves the following steps:

1. **Enhance generateStaticParams**: Update the generateStaticParams function to better support dynamic server-side features like next/headers. This could involve adding a new parameter to the function that allows developers to specify whether dynamic parameters should be allowed.

2. **Improve Error Handling**: Implement a more sophisticated error handling mechanism that provides clearer error messages and guidance on how to use generateStaticParams and next/headers together effectively.

3. **Server Components Updates**: Update server components to support more flexible and dynamic server usage. This could involve changes to how Next.js handles server-side rendering and static site generation.

4. **Documentation and Examples**: Provide clearer documentation and examples on how to use generateStaticParams and next/headers together effectively.

## Drawbacks

The proposed solution may have the following drawbacks:

1. **Complexity**: The changes required to address this issue may add complexity to the Next.js codebase.

2. **Backward Compatibility**: The solution may require changes to existing codebases that use generateStaticParams and next/headers, which could impact backward compatibility.

## Alternatives

Alternative solutions could include:

1. **Disabling next/headers in generateStaticParams**: One possible solution is to disable next/headers when using generateStaticParams. However, this may limit the flexibility of server-side rendering.

2. **Using a Different Approach**: Another possible solution is to use a different approach to achieve the desired functionality, such as using a different library or framework.

## Unresolved Questions

The following questions remain unresolved:

1. **Implementation Details**: The implementation details of the proposed solution are still unclear and require further discussion.

2. **Impact on Performance**: The impact of the proposed solution on performance is still unclear and requires further benchmarking and testing.

3. **Community Feedback**: The proposed solution requires community feedback and validation to ensure that it meets the needs of Next.js users.

---

*RFC generated by Resurrection Bot 🧬*
