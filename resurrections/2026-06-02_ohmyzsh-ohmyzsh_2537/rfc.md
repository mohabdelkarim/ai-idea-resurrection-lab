# RFC: No, I dont want share history.

## 1. Summary

This RFC proposes adding a configuration option to toggle the `share_history` option in ohmyzsh. The goal is to provide users with an easy way to control whether or not their command history is shared across different zsh sessions.

## 2. Motivation

The `share_history` option in zsh allows command history to be shared across different zsh sessions. While this can be useful for some users, others may prefer to keep their command history private. Currently, ohmyzsh enables `share_history` by default, and users who want to disable it have to manually modify their zsh configuration files.

## 3. Detailed Design

To implement this feature, we propose adding a new configuration option to ohmyzsh. This option will be called `share_history` and will be enabled by default to maintain the current behavior of ohmyzsh.

The implementation will involve the following steps:

* Add a new option to the ohmyzsh configuration file. This option will be a boolean value that indicates whether or not `share_history` should be enabled.
* Modify the ohmyzsh library to check the value of this option and set the `share_history` option accordingly.
* Update the ohmyzsh documentation to reflect the new configuration option.

## 4. Drawbacks

One potential drawback of this feature is that it may cause issues with users who are already using `share_history` and expect it to be enabled by default. However, this can be mitigated by clearly documenting the change and providing guidance on how to opt-out of the new behavior.

## 5. Alternatives

One alternative to adding a new configuration option is to simply recommend that users append `unsetopt share_history` to their zshrc file. However, this approach requires manual intervention and is not a straightforward fix. Another alternative is to use a zsh hook to toggle the `share_history` option, but this approach may be more complex and harder to maintain.

## 6. Unresolved Questions

One unresolved question is how to handle the case where a user sets `share_history` to a non-boolean value. We will need to decide how to handle this case and provide clear error messages to users who make this mistake.

Another unresolved question is whether or not to add similar configuration options for other zsh options that are currently enabled by default in ohmyzsh. We will need to consider the trade-offs between providing more flexibility to users and adding complexity to the ohmyzsh configuration.

---

*RFC generated by Resurrection Bot 🧬*
