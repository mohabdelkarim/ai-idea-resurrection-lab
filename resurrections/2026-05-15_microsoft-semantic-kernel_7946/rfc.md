# RFC: Python: .Net: OpenAI - Support response_format type of json_schema (Structured Outputs)

### Summary

This RFC proposes adding support for OpenAI's response_format type json_schema to enforce JSON output validation in the Semantic Kernel. The goal is to enhance the OpenAI connector by introducing a structured output feature, allowing users to define a JSON schema that the model output must adhere to.




### Motivation

The increasing adoption of OpenAI's structured outputs and the growing importance of JSON schema validation in AI applications make this feature a valuable addition to the Semantic Kernel. With tools like Pydantic for Python and .NET's built-in JSON serialization APIs, integrating this feature becomes more feasible. The Semantic Kernel's unified interface for AI connectors makes this feature a natural fit, enabling more robust and reliable interactions with AI models.




### Detailed Design

To implement this feature, we propose introducing a new `ResponseFormat` class with `Type` and `Schema` properties. This class will be used in the OpenAI connector to pass the `json_schema` and `schema` properties when requesting structured outputs.

- The `ResponseFormat` class will have two properties:
  - `Type`: A string that specifies the type of response format (e.g., json_schema).
  - `Schema`: A JSON object that defines the schema for the structured output.

- The OpenAI connector will be updated to support the `ResponseFormat` class, allowing users to specify a JSON schema for the model output.

- The .NET client will be updated to handle the new response format, potentially using .NET's built-in JSON serialization APIs to validate the output against the provided schema.

- The Semantic Kernel's abstractions will be extended to accommodate this new feature in a way that is consistent with existing AI connector interfaces.




### Drawbacks

1. **Complexity**: Adding support for JSON schema validation may introduce additional complexity to the OpenAI connector and the Semantic Kernel's abstractions.

2. **Performance**: Validating the model output against a JSON schema may incur a performance overhead, potentially impacting the overall latency of AI requests.

3. **Compatibility**: Ensuring compatibility with different JSON schema validation libraries (e.g., Pydantic for Python) and .NET's built-in JSON serialization APIs may require additional effort.




### Alternatives

1. **External Libraries**: Instead of integrating JSON schema validation directly into the Semantic Kernel, consider using external libraries like Pydantic for Python or .NET's built-in JSON serialization APIs. This approach may simplify the implementation but could add dependencies to the project.

2. **Post-processing**: Another approach is to perform JSON schema validation outside of the Semantic Kernel, as a post-processing step. This approach may be less efficient and less convenient for users.




### Unresolved Questions

1. **Schema Validation Libraries**: Which JSON schema validation libraries should be supported for Python and .NET? Should we use Pydantic for Python and .NET's built-in JSON serialization APIs?

2. **Error Handling**: How should errors during JSON schema validation be handled? Should validation errors be propagated to the user or handled internally by the Semantic Kernel?

3. **Performance Optimization**: How can we optimize the performance of JSON schema validation to minimize the impact on AI request latency?

---

*RFC generated by Resurrection Bot 🧬*
