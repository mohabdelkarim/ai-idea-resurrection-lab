# Analysis: Python: .Net: OpenAI - Support response_format type of json_schema (Structured Outputs)

> Add support for OpenAI's response_format type json_schema to enforce JSON output validation.

**Why it will work now:** This feature is crucial for applications relying on JSON output and deserialization to strongly-typed .NET types.

---

## Why It Died

The issue was likely abandoned due to lack of immediate priority, given that it was filed relatively recently and might have been overshadowed by other tasks. The repository's activity suggests that the feature, while interesting, did not have a champion to drive it forward. Additionally, implementing support for a new OpenAI feature requires careful consideration of the Semantic Kernel's abstractions and .NET client.

## Why 2026 Changes Everything

With the growing adoption of OpenAI's structured outputs and the increasing importance of JSON schema validation in AI applications, the need for this feature has become more pressing. The existence of tools like Pydantic and .NET's built-in JSON serialization APIs makes integration more feasible. Furthermore, the Semantic Kernel's goal of providing a unified interface for AI connectors makes this feature a natural fit.

## Modern Architecture

The feature could be implemented by introducing a new `ResponseFormat` class with `Type` and `Schema` properties. This class would be used in the OpenAI connector to pass the `json_schema` and `schema` properties when requesting structured outputs. The .NET client would need to be updated to handle the new response format, potentially using .NET's built-in JSON serialization APIs to validate the output against the provided schema. The Semantic Kernel's abstractions would need to be extended to accommodate this new feature in a way that is consistent with existing AI connector interfaces.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~80 hours |
| 🏷️ Tech Tags | openai, .net, json, semantic kernel, ai connector |
| 💀 Year Abandoned | 2025 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
