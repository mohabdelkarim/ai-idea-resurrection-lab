# Analysis: Ignore changes for a single AWS Resource Tag

> This feature provides the ability to ignore changes for specific AWS resource tags, allowing for more fine-grained control over resource tag management and enabling users...

**Why it will work now:** The proposed feature is necessary because it addresses a significant limitation in the current implementation of the ignore_changes argument in Terraform, which only allows users...

---

## Why It Died

The feature to ignore changes for a single AWS Resource Tag was not implemented due to the lack of a flexible ignore_changes configuration in Terraform at the time. The VS Code extension API also lacked the necessary FileDecorationProvider until 2019, which made it difficult to integrate this feature with the Terraform editor. Additionally, the Terraform configuration language, HCL, did not have built-in support for conditional logic, making it hard to implement this feature. The necessary AWS API support for filtering resource tags was also not fully available until later.

## Why 2026 Changes Everything

The recent advancements in Terraform's HCL language, including support for conditional logic and loops, make it possible to implement this feature. The introduction of the ignore_changes argument in Terraform 1.0, combined with the AWS API support for filtering resource tags, provides the necessary building blocks for this feature. Additionally, the widespread adoption of Terraform and the growth of the AWS ecosystem have increased the demand for this feature, making it more viable for implementation.

## Modern Architecture

To implement this feature, we would use a combination of Terraform's HCL language and the AWS API. We would introduce a new configuration option, ignore_tag_changes, which would allow users to specify a list of tags to ignore changes for. We would then use the AWS API to filter the resource tags and only update the tags that are not ignored. We would also use Terraform's built-in support for conditional logic to handle cases where the ignore_tag_changes option is not specified. The design would involve creating a new Terraform resource, aws_resource_tag_ignore, which would handle the ignore_tag_changes configuration and interact with the AWS API to filter the resource tags. We would use the Terraform provider architecture to integrate this feature with the AWS provider, and we would use the HCL language to define the configuration options and the logic for ignoring tag changes. This would involve creating a new schema for the aws_resource_tag_ignore resource, which would include the ignore_tag_changes option. We would also need to update the existing Terraform resources to support the new ignore_tag_changes option.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 5/10 |
| ⏱️ Effort Estimate | ~120 hours |
| 🏷️ Tech Tags | terraform, aws, hcl, go |
| 💀 Year Abandoned | 2019 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
