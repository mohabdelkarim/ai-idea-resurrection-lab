# Analysis: Optional arguments in object variable type definition

> Add support for optional arguments in object variable type definitions in Terraform.

**Why it will work now:** This feature improves Terraform's flexibility and usability by allowing for more expressive and robust variable type definitions.

---

## Why It Died

The issue was not prioritized and got lost in the backlog. The feature request was not addressed in the 0.12 release and subsequent releases focused on other priorities.

## Why 2026 Changes Everything

With the introduction of Terraform 1.0 and the increased adoption of Terraform in large-scale infrastructures, the need for more flexible and robust variable type definitions has become more pressing. The recent improvements in Terraform's type system, such as the introduction of `optional` and `nullable` types in other contexts, lay the groundwork for addressing this issue.

## Modern Architecture

The modern design would involve introducing an `optional` keyword for object properties, allowing users to define optional arguments with clear types. This would integrate with Terraform's existing type system, potentially leveraging recent improvements in type checking and validation. Classes such as `ObjectType` and `Property` in Terraform's configuration parser would need to be updated to accommodate this feature.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | terraform, configuration, type-system |
| 💀 Year Abandoned | 2019 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
