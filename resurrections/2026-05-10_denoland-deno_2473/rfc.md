# RFC: 1.0

## 1. Summary

This RFC proposes the revival of Deno's 1.0 release, made possible by recent advancements in modern tooling and APIs. The main feature that was missing for the 1.0 release was dev tool support, specifically integration with Chrome DevTools. With the maturity of Rusty_v8 and improvements in V8InspectorClient, we can now implement this feature and finally achieve the 1.0 release.

## 2. Motivation

The 1.0 release of Deno was delayed multiple times due to the lack of dev tool support. The team aimed to implement this feature by rewriting the bindings to V8, but the work was not completed on time. Other issues like source map correctness, double downloads, and cache refactoring also needed attention. With the current advancements in tooling and APIs, we can now complete the dev tool support and achieve the 1.0 release.

## 3. Detailed Design

The modern design for Deno 1.0 would involve a websocket server in Rust that forwards messages to V8 using V8InspectorClient. This would enable debugging using Chrome DevTools. The architecture would include:

* A refactored cache system to handle efficient module loading and execution
* Correct source map handling to ensure accurate debugging
* A websocket server in Rust that forwards messages to V8 using V8InspectorClient
* A microservices pattern with a focus on modularity and scalability

The design would leverage the maturity of Rusty_v8 and improvements in V8InspectorClient to implement the dev tool support.

## 4. Drawbacks

The main drawback of this proposal is the complexity of implementing the dev tool support. The team would need to invest significant time and effort into rewriting the bindings to V8 and implementing the websocket server in Rust. Additionally, there may be potential performance implications of integrating with Chrome DevTools.

## 5. Alternatives

An alternative approach would be to use existing tooling solutions, such as using a third-party library or service to provide dev tool support. However, this would likely limit the customization and control that the Deno team has over the feature. Another alternative would be to delay the 1.0 release further, but this would continue to hinder the adoption and growth of the Deno ecosystem.

## 6. Unresolved Questions

The following questions remain unresolved:

* What is the exact timeline for implementing the dev tool support?
* What are the potential performance implications of integrating with Chrome DevTools?
* How will the Deno team ensure the stability and reliability of the websocket server in Rust?
* What are the plans for future maintenance and updates of the dev tool support?

These questions would need to be addressed and resolved before the 1.0 release can be achieved.

---

*RFC generated by Resurrection Bot 🧬*
