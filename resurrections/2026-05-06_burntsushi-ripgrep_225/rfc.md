# RFC: support searching compressed files using in-process decompression

## Summary

This RFC proposes the addition of in-process decompression support for gzip, bzip2, and xz compressed files in ripgrep. The goal is to enable searching compressed files without requiring external decompression tools. This feature will improve the usability and flexibility of ripgrep.

## Motivation

Currently, ripgrep does not support searching compressed files directly. Users have to decompress files manually before searching. This can be inconvenient and time-consuming, especially when dealing with large compressed files. By implementing in-process decompression, ripgrep can seamlessly handle compressed files, making it a more powerful and user-friendly tool.

## Detailed Design

The proposed implementation involves the following steps:

* Introduce a new `Decompressor` trait that defines the decompression interface. This trait will have methods for decompressing data and checking the compressed file type.
* Provide concrete implementations of the `Decompressor` trait for gzip, bzip2, and xz formats using the `flate2`, `bzip2-rs`, and `xz2` libraries, respectively.
* Update the `Searcher` struct to use the `Decompressor` trait. This will allow the `Searcher` to handle compressed files seamlessly.
* Implement file type detection using the `file_type` crate or by checking the magic numbers of the compressed files.

The `Decompressor` trait will be defined as follows:

```rust
trait Decompressor {
    fn decompress(&self, data: &[u8]) -> Result<Vec<u8>, std::io::Error>;
    fn file_type(&self, data: &[u8]) -> bool;
}
```

Concrete implementations of the `Decompressor` trait will be provided for each compressed file format. For example, the implementation for gzip will use the `flate2` library:

```rust
struct GzipDecompressor;

impl Decompressor for GzipDecompressor {
    fn decompress(&self, data: &[u8]) -> Result<Vec<u8>, std::io::Error> {
        let mut decoder = flate2::read::GzDecoder::new(data);
        let mut decompressed_data = Vec::new();
        decoder.read_to_end(&mut decompressed_data)?;
        Ok(decompressed_data)
    }

    fn file_type(&self, data: &[u8]) -> bool {
        data.starts_with(b'x1fx8b')
    }
}
```

## Drawbacks

The main drawback of this implementation is the potential performance impact of in-process decompression. Decompressing large files can be computationally expensive and may slow down the search process. However, this can be mitigated by using efficient decompression libraries and optimizing the implementation.

Another potential drawback is the added complexity of handling compressed files. This can be addressed by carefully designing the `Decompressor` trait and its implementations to ensure that they are easy to use and maintain.

## Alternatives

An alternative approach is to use external decompression tools, such as `gzip` or `xz`, to decompress files before searching. However, this approach requires users to have these tools installed and configured properly, which can be inconvenient.

Another alternative is to use a library that provides a unified interface for decompressing various file formats, such as `archive` or `compression`. However, these libraries may not provide the same level of performance or flexibility as using specialized libraries like `flate2` or `xz2`.

## Unresolved Questions

* How will the `Searcher` struct handle errors that occur during decompression?
* How will the `Decompressor` trait be used to detect the compressed file type?
* How will the implementation handle large compressed files that do not fit into memory?

---

*RFC generated by Resurrection Bot 🧬*
