# Analysis: support searching compressed files using in-process decompression

> Ripgrep should support searching compressed files using in-process decompression for gzip, bzip2, and xz formats.

**Why it will work now:** Ripgrep can now efficiently search compressed files with in-process decompression, preserving file names in output.

---

## Why It Died

The issue was abandoned likely due to the lack of a clear implementation plan and potential performance concerns. The feature request was not prioritized among other tasks. Additionally, implementing in-process decompression required integrating with existing decompression libraries. The issue was labeled as 'icebox', indicating it was not a high priority.

## Why 2026 Changes Everything

With the existence of libraries like `flate2` and `bzip2-rs` for Rust, implementing in-process decompression is more feasible. The `xz2` crate also provides an interface for xz decompression. These libraries provide a stable and efficient way to handle compressed files.

## Modern Architecture

The feature can be implemented by introducing a new `Decompressor` trait that defines the decompression interface. Concrete implementations for gzip, bzip2, and xz can be provided using the respective libraries. The `Searcher` struct can be updated to use the `Decompressor` trait, allowing it to handle compressed files seamlessly. The file type detection can be done using the `file_type` crate or by checking the magic numbers of the compressed files.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~80 hours |
| 🏷️ Tech Tags | compression, decompression, grep, search |
| 💀 Year Abandoned | 2018 |
| 🔬 Has PoC | No |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
