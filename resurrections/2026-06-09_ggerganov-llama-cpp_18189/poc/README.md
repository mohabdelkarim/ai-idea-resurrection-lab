# Proof of Concept: Feature Request: Idle model unload timeout (router mode / config.ini)

**Language:** python
**Estimated run time:** < 5 minutes

## Prerequisites

- Python 3.12+, pip install requirements

## How to Run

```bash
pip install -r requirements.txt
python main.py
```

## What This Demonstrates

Implement an idle model unload timeout feature in llama-server to free up resources.
