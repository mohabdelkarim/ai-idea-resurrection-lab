# RFC: proposal: Go 2: string interpolation

## Summary

This RFC proposes the addition of string interpolation to the Go programming language, aiming to enhance type safety and performance in string formatting. The proposed feature would allow developers to embed expressions within string literals, making the code more readable and expressive.

## Motivation

The current string formatting mechanisms in Go, such as `fmt.Sprintf` and `fmt.Printf`, provide a way to format strings but lack the expressiveness and readability of string interpolation. Other languages have successfully implemented string interpolation, demonstrating its value in improving code readability and reducing errors. With the evolution of Go's ecosystem, particularly with the introduction of Go 1.19's generics and improved error handling, the landscape for string interpolation has become more favorable.

## Detailed Design

The proposed design involves introducing a new syntax for string interpolation, using a syntax similar to `$variable` or `{variable}`. This syntax would be supported in string literals, allowing developers to embed expressions directly within the string. The design also includes support for formatting options, which could be achieved through a new package in the Go standard library or enhancements to the `fmt` package.

The interpolation syntax would be as follows:

- `$variable`: Inserts the value of `variable` into the string.
- `${variable}`: Inserts the value of `variable` into the string, allowing for more complex expressions.
- `${variable:format}`: Inserts the value of `variable` into the string, formatted according to the specified format.

The implementation would leverage Go's generics for type-safe formatting, ensuring that the interpolated values are correctly typed and reducing the risk of errors.

## Drawbacks

The main drawbacks of this proposal are:

- **Compatibility concerns**: Changing the syntax of string literals could break existing code, requiring a transition period or tooling to assist with updates.
- **Complexity of implementation**: Implementing string interpolation would require changes to the Go compiler and potentially the standard library, adding complexity to the language.
- **Limited formatting methods supported**: The initial implementation might not support all formatting methods available in other languages or libraries, potentially limiting its usefulness.

## Alternatives

Alternatives to string interpolation include:

- **Using existing formatting functions**: Developers could continue using `fmt.Sprintf` and similar functions, which are well-established but less expressive.
- **External libraries**: Developers could use external libraries that provide string interpolation functionality, but this would not be a standard solution.

## Unresolved Questions

- **Syntax and semantics**: The exact syntax and semantics of string interpolation in Go are still to be determined, requiring further discussion and experimentation.
- **Integration with existing libraries and frameworks**: How string interpolation would integrate with existing libraries and frameworks, such as `fmt` and popular third-party packages, is still unclear.
- **Error handling and edge cases**: How errors would be handled, and what edge cases would need to be considered (e.g., null or undefined values), are still to be resolved.

---

*RFC generated by Resurrection Bot 🧬*
