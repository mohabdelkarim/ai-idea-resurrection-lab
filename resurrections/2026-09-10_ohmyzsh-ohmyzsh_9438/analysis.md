# Analysis: Git plugin: "gl" should be short for "git log", not "git push"

> The Git plugin should map the short alias “gl” to “git log” instead of “git push”.

**Why it will work now:** zsh 5.9 introduced associative arrays enabling clearer alias definitions for plugins.

---

## Why It Died

The proposal conflicted with existing alias conventions in the git plugin, and maintainers prioritized other feature work. No consensus was reached on renaming because many users relied on the current "gl" shortcut for push. Limited reviewer bandwidth and the lack of automated tests for alias behavior slowed progress.

## Why 2026 Changes Everything

Zsh 5.9 introduced robust associative array support, simplifying alias management in plugins. The ohmyzsh community now uses automated CI linting for plugin consistency, reducing risk of breaking changes. Improved documentation tooling makes updating alias tables straightforward, enabling safe refactoring of "gl".

## Modern Architecture

Implement a mapping table within the git plugin using Zsh associative arrays, where keys are short aliases and values are full git commands. Create a function update_git_aliases() that iterates over the table and registers each alias via the builtin alias command. Separate the alias definitions into a dedicated file (git_aliases.zsh) to keep plugin logic clean. Add unit tests using the zsh-test framework to verify that "gl" resolves to "git log" and that other aliases remain unchanged. Document the change in the plugin README with examples and migration instructions. Ensure backward compatibility by providing a deprecation warning for the old "gl" mapping before removal in a future release.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~12 hours |
| 🏷️ Tech Tags | git |
| 💀 Year Abandoned | 2025 |
| 🔬 Has PoC | No |
| 📋 Has RFC | No |

---

*Analysis generated by Resurrection Bot 🧬*
