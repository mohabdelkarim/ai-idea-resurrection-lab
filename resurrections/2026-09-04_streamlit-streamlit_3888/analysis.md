# Analysis: Give each element a unique HTML `id` or `class`

> Streamlit should assign a unique HTML id or class to each rendered element for easier targeting.

**Why it will work now:** The introduction of Streamlit’s component API v2 and improved React‑based rendering enables unique IDs.

---

## Why It Died

The feature required deep changes to Streamlit's rendering pipeline, which lacked a stable internal API for element identifiers. At the time, the component framework was still evolving, making it risky to expose IDs. Maintaining backward compatibility with existing apps added further hesitation. Limited contributor bandwidth and priority given to core functionality delayed implementation. Additionally, there were concerns about potential CSS conflicts and security implications of exposing IDs.

## Why 2026 Changes Everything

Streamlit’s component API v2, released in 2023, provides a stable hook for passing metadata to the frontend. The underlying React‑based renderer now supports deterministic key generation, simplifying unique ID assignment. Modern CSS-in-JS libraries and the widespread adoption of the Shadow DOM enable safe scoping of generated IDs. Enhanced testing tools like Playwright make validating element selectors straightforward. These advances collectively remove the technical blockers that existed in 2021.

## Modern Architecture

Introduce an ElementIdentifier class in the Python core that generates a UUID‑based id for each widget instance. Extend the existing DeltaGenerator to accept an optional identifier parameter and propagate it to the frontend via the protobuf message. On the React side, add an IdentifierProvider component that injects the id as a data‑attribute on the root DOM node of each widget. Update the component API to expose the identifier to custom components, allowing them to forward it to their internal markup. Ensure backward compatibility by defaulting to autogenerated ids when none are provided. Add unit tests for the identifier generation and integration tests using Playwright to verify selector stability. Document the new feature in the styling guide and provide examples for custom CSS and JavaScript targeting.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~70 hours |
| 🏷️ Tech Tags | react, css |
| 💀 Year Abandoned | 2024 |
| 🔬 Has PoC | No |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
