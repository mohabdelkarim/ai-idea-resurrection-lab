# RFC: Proposal: Model v2, program context

## Summary

The Bubble Tea Model v2 proposal aims to enhance the program context and model interface for better terminal interaction and modern Go programming practices. This update will introduce a new `Context` type that embeds `context.Context`, allowing for improved control over goroutine execution and terminal interactions. The `Model` interface will be updated to accept this `Context` in its `Init`, `Update`, and `View` methods.

## Motivation

The existing model and context interfaces in Bubble Tea have been sufficient for many use cases but are showing signs of limitation as the project and its ecosystem evolve. With advancements in terminal capabilities and the growing popularity of Bubble Tea, there is a need for a more robust and flexible model and context interface. The existence of libraries like Lip Gloss and the increasing adoption of Bubble Tea in various projects, including remote sessions (e.g., Wish), make the proposed changes more relevant.

## Detailed Design

The proposed Model v2 and program context will utilize modern Go programming practices, including the use of interfaces and contexts. The `Context` type will embed `context.Context`, allowing for better control over goroutine execution and terminal interactions. This `Context` will also provide access to terminal-specific information and actions, such as Kitty Keyboard support and terminal background color detection.

The `Model` interface will be updated to accept a `Context` in its `Init`, `Update`, and `View` methods. This will enable more flexible and powerful terminal interactions, aligning with contemporary Go standards and leveraging the language's built-in concurrency features.

## Drawbacks

The main drawback of this proposal is the potential complexity in implementation and the learning curve for existing users. The changes are significant, and without a clear path forward, the proposal might stall again. Additionally, there might be backward compatibility issues that need to be addressed.

## Alternatives

An alternative approach could be to introduce a new, optional interface for models that require advanced context and terminal interaction capabilities, while keeping the existing model interface for simpler use cases. However, this might lead to fragmentation and confusion among users.

## Unresolved Questions

- How will we handle backward compatibility, and what are the specific changes needed in existing models?
- What are the exact terminal features and APIs that will be exposed through the new `Context` type?
- How will we ensure that the implementation does not overly complicate the library for simple use cases?

---

*RFC generated by Resurrection Bot 🧬*
