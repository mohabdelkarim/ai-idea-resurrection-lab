# Proof of Concept: ActiveRecord enum: use validation if exists instead of raising ArgumentError

**Language:** python
**Estimated run time:** < 5 minutes

## Prerequisites

- Python 3.12+, pip install requirements

## How to Run

```bash
pip install -r requirements.txt
python main.py
```

## What This Demonstrates

Prefer validation over ArgumentError for enum values in ActiveRecord.
