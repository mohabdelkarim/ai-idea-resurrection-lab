# Analysis: proposal: spec: allow x, y..., z in list for variadic function argument

> Allow mixing explicit arguments with a spliced slice in variadic function calls using ellipsis syntax.

**Why it will work now:** Go 1.18 generics and enhanced go/ast tooling now enable safe implementation of variadic splicing.

---

## Why It Died

The proposal conflicted with Go's design principle of keeping variadic arguments simple and explicit, leading to debates about readability. Compiler complexity would increase due to needing to splice slices at call sites, which the core team deemed risky. Community bandwidth was limited as focus shifted to generics and module improvements, leaving little time for language syntax extensions. Additionally, the lack of a clear use‑case beyond niche patterns made it hard to prioritize. Finally, the existing rule preventing slice splicing avoided subtle bugs, reinforcing the status quo.

## Why 2026 Changes Everything

Go 1.18 introduced generics, providing more expressive type handling that can emulate variadic splicing without syntax changes. The go/ast package was enhanced in Go 1.20, allowing easier manipulation of call expressions for future compiler experiments. Improved compiler diagnostics in Go 1.22 make detecting ambiguous variadic usage safer, reducing the risk of subtle bugs. These concrete language and tooling upgrades lower the cost of implementing the proposal. Consequently, the technical barriers that existed in 2017 are now largely mitigated.

## Modern Architecture

Implement a new AST node variant VariadicSpliceExpr that represents a slice followed by ellipsis within an argument list. Extend the parser to recognize this pattern and construct the node, reusing existing token handling logic. In the type checker, treat VariadicSpliceExpr as producing a slice of the variadic element type, ensuring compatibility with the function's parameter type. The compiler backend will generate code that creates a temporary slice, copies the explicit arguments, and appends the spliced slice using the runtime's slice append routine. Add a compiler flag goexperiment.VariadicSplice to gate the feature, allowing gradual rollout and testing. Update gofmt to format the new syntax consistently, and extend go vet with checks for ambiguous usage. Documentation and test suites will cover edge cases such as empty slices and mixed explicit arguments.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 8/10 |
| ⏱️ Effort Estimate | ~72 hours |
| 🏷️ Tech Tags | go, compiler, generics |
| 💀 Year Abandoned | 2024 |
| 🔬 Has PoC | No |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
