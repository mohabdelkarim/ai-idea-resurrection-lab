# Analysis: capture output of provisioners into variables

> Capture the output of provisioners into Terraform variables for use in other resources.

**Why it will work now:** This feature is now feasible due to Terraform 1.0's improved provisioner system and growing demand for dynamic environments.

---

## Why It Died

The issue was abandoned due to lack of clear use cases and implementation challenges. At the time, Terraform's provisioner system was not well-suited for capturing output. The core team had to prioritize other features. Additionally, there were concerns about introducing complexity and potential security risks.

## Why 2026 Changes Everything

With the introduction of Terraform 1.0 and the new provisioner system, the groundwork has been laid for capturing provisioner output. The Terraform CDK (Cloud Development Kit) and the Terraform CLI's new JSON output format provide a clear path forward. Furthermore, the growing use of Terraform in complex, dynamic environments makes this feature more valuable.

## Modern Architecture

The feature could be implemented by introducing a new data structure to store provisioner output, which would be accessible through a built-in data source. This would involve modifying the provisioner runtime to capture output and the Terraform state to store it. The output could be made available through a new `provisioner_output` data source, similar to the existing `locals` and `variables` data sources. The implementation would require careful consideration of security and data validation.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~120 hours |
| 🏷️ Tech Tags | terraform, provisioners, variables |
| 💀 Year Abandoned | 2014 |
| 🔬 Has PoC | No |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
