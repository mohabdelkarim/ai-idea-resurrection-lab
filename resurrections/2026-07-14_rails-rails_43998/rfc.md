# RFC: Rails cashes with Ruby 3.1.0 release

### Summary

This RFC proposes a solution to address the compatibility issues between Rails 7.0.0 and Ruby 3.1.0, which cause Rails to crash during project creation. The proposed solution involves updating the railtie and engine modules in Rails to ensure compatibility with Ruby 3.1.




### Motivation

The release of Ruby 3.1.0 has introduced compatibility issues with Rails 7.0.0, leading to a crash during project creation. The error message indicates that Rails::Engine is abstract and cannot be instantiated directly. This issue likely arose due to changes in Ruby 3.1.0 that affected how Rails 7.0.0 operates. With the release of Ruby 3.1.1 and later, which include various bug fixes and compatibility improvements, this compatibility issue may now be resolvable.




### Detailed Design

To address this issue, the following steps are proposed:

1. **Update Railtie Module**: Update the railtie module in Rails to ensure compatibility with Ruby 3.1. This may involve adjusting how Rails::Railtie is initialized and ensuring that all dependencies are correctly loaded.

2. **Update Engine Module**: Update the engine module in Rails to ensure compatibility with Ruby 3.1. This may involve adjusting how Rails::Engine is initialized and ensuring that all dependencies are correctly loaded.

3. **Incorporate Ruby 3.1's Improved Error Handling**: Incorporate Ruby 3.1's improved error handling and deprecation warnings to help prevent similar issues in the future.

4. **Add Test Coverage**: Add test coverage to ensure that Rails works correctly with Ruby 3.1 and later.




### Drawbacks

The proposed solution may have the following drawbacks:

1. **Backward Compatibility**: The updates to the railtie and engine modules may affect backward compatibility with earlier versions of Ruby.

2. **Performance Impact**: The updates may have a performance impact on Rails applications.

3. **Additional Testing**: Additional testing may be required to ensure that the updates do not introduce new issues.




### Alternatives

The following alternatives were considered:

1. **Downgrade Ruby**: Downgrade Ruby to a version that is compatible with Rails 7.0.0.

2. **Use a Patch**: Use a patch to fix the compatibility issue.

However, these alternatives are not recommended as they may introduce additional issues or limit the ability to take advantage of new features in Ruby 3.1.




### Unresolved Questions

The following questions remain unresolved:

1. **Impact on Other Ruby Versions**: What is the impact of the proposed solution on other versions of Ruby?

2. **Performance Impact**: What is the performance impact of the proposed solution on Rails applications?

3. **Additional Testing**: What additional testing is required to ensure that the proposed solution does not introduce new issues?

---

*RFC generated by Resurrection Bot 🧬*
