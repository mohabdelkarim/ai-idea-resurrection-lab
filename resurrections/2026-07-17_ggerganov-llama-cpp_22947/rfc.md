# RFC: Feature Request: Add MTP / speculative decoding support to llama-bench for PR #22673

## Summary

This RFC proposes the addition of MTP (Multi-Token Prediction) and speculative decoding support to `llama-bench` for easier benchmarking. The goal is to enable the use of speculative decoding flags in `llama-bench`, similar to those already available in `llama-cli` and `llama-server`, to facilitate more comprehensive benchmarking.

## Motivation

The motivation behind this proposal is to enhance the benchmarking capabilities of `llama-bench` by incorporating speculative decoding and MTP support. This will allow users to easily benchmark and evaluate the performance of different models with speculative decoding enabled. The recent advancements in the `ggml-org/llama.cpp` repository, particularly the draft MTP PR #22673, have laid the groundwork for integrating this feature into `llama-bench`.

## Detailed Design

To implement this feature, `llama-bench` will adopt the shared argument parsing infrastructure used by `llama-cli` and `llama-server`. This will involve refactoring the current parser and benchmark loop in `llama-bench` to accommodate the speculative decoding flags. The implementation will utilize the existing flag handling mechanisms, such as:

* `--spec-type mtp`
* `--spec-draft-n-max`
* `--spec-draft-ngl`

These flags will enable MTP benchmarking and allow users to specify the number of draft tokens and the number of GPU layers for speculative decoding.

## Drawbacks

The main drawback of this proposal is the potential complexity introduced by adding speculative decoding and MTP support to `llama-bench`. This may require additional testing and validation to ensure that the feature works correctly and does not impact the performance of existing benchmarks.

## Alternatives

An alternative approach would be to use custom scripts or `llama-server` setups to achieve speculative decoding and MTP benchmarking. However, this would require additional effort and expertise from users, and may not provide the same level of convenience and ease of use as integrating the feature directly into `llama-bench`.

## Unresolved Questions

The following questions remain to be addressed:

* How will the speculative decoding flags be validated and handled in `llama-bench`?
* What are the performance implications of enabling speculative decoding and MTP support in `llama-bench`?
* How will users be able to specify and configure the speculative decoding flags in `llama-bench`?

These questions will need to be addressed during the implementation phase to ensure that the feature is properly integrated and functions as expected.

---

*RFC generated by Resurrection Bot 🧬*
