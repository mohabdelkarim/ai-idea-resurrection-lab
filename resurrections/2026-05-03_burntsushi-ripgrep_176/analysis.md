# Analysis: support searching across multiple lines

> Ripgrep gains support for searching across multiple lines using regex.

**Why it will work now:** Ripgrep's improved search engine and Rust's regex library make this feature feasible now.

---

## Why It Died

The issue died due to the complexity of implementing multiline regex support in ripgrep. At the time, the Rust regex library lacked efficient support for multiline matching. The VS Code extension API also lacked the necessary hooks for custom regex handling. Additionally, the focus was on optimizing single-line search performance.

## Why 2026 Changes Everything

The introduction of Rust's regex 1.6 library, which includes improved support for multiline matching, and the advancements in ripgrep's own search engine, make it feasible to implement this feature. The increased adoption of ripgrep as a daily tool also justifies the investment in this feature. Furthermore, the growth of the Rust ecosystem has led to more efficient and performant libraries that can be leveraged.

## Modern Architecture

To implement multiline regex support, we would utilize Rust's regex library and create a custom search engine that can handle multiline patterns. We would introduce a new `MultilineSearcher` struct that would wrap the existing `Searcher` and provide the necessary logic for multiline matching. The `MultilineSearcher` would use a combination of the `regex` library and ripgrep's own search engine to efficiently search for multiline patterns. This would involve modifying the `search` function to handle multiline patterns and updating the `Searcher` trait to include multiline search capabilities.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~80 hours |
| 🏷️ Tech Tags | rust, regex, search, ripgrep |
| 💀 Year Abandoned | 2021 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
