# RFC: support searching across multiple lines

## Summary
   This RFC proposes the addition of multiline regex support to ripgrep. This feature would allow users to search for patterns that span multiple lines.

   ## Motivation
   The ability to search for multiline patterns is a common request from users. This feature would make ripgrep more versatile and useful for a wider range of use cases.

   ## Detailed Design
   The implementation would involve creating a custom search engine that can handle multiline patterns. This would require modifying the existing `Searcher` and introducing a new `MultilineSearcher` struct.

   ## Drawbacks
   The addition of multiline regex support may introduce performance overhead. This could be mitigated by optimizing the search engine and using efficient algorithms.

   ## Alternatives
   One alternative is to use an external tool that supports multiline regex, such as `grep -z`. However, this would not be as convenient or integrated as having the feature built into ripgrep.

   ## Unresolved Questions
   The exact performance impact of this feature is unknown and would require further testing and benchmarking. Additionally, the API for this feature would need to be designed and implemented.

---

*RFC generated by Resurrection Bot 🧬*
