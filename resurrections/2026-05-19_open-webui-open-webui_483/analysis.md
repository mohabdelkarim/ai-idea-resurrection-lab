# Analysis: feat: OAuth/OIDC 

> Implement OAuth/OIDC authentication to enable account creation via popular auth providers.

**Why it will work now:** With libraries like NextAuth.js and widespread OIDC adoption, secure and flexible authentication is now feasible.

---

## Why It Died

The OAuth/OIDC feature request likely stalled due to the complexity of integrating with various auth providers, limited resources, and prioritization of core features. Additionally, implementing a secure and flexible authentication system requires careful consideration of multiple factors, such as token handling, user mapping, and logout functionality. The lack of a clear implementation plan and potential security concerns may have contributed to its abandonment. The feature's non-core status might have also led to it being deprioritized. As a result, the issue remained open without a concrete solution.

## Why 2026 Changes Everything

The landscape has changed significantly since the issue was filed. New tools and APIs have emerged, making it easier to implement OAuth/OIDC. For example, libraries like NextAuth.js provide a simple and secure way to handle authentication with multiple providers. Furthermore, OpenID Connect (OIDC) has gained widespread adoption, and many auth providers now offer OIDC-compliant interfaces. These advancements can simplify the integration process and make the feature more feasible.

## Modern Architecture

The modern design for this feature would involve using a library like NextAuth.js to handle authentication with various OAuth/OIDC providers. The system would consist of an AuthProvider class that abstracts the authentication logic, a UserService class that manages user data, and an AuthController class that handles authentication flows. The design would follow the Adapter pattern to accommodate different auth providers. Additionally, the system would utilize OIDC-compliant APIs to ensure secure and standardized authentication.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~120 hours |
| 🏷️ Tech Tags | oauth, oidc, authentication, typescript |
| 💀 Year Abandoned | 2025 |
| 🔬 Has PoC | No |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
