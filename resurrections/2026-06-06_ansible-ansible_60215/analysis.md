# Analysis: Support running ansible-test on collections outside a collection root

> Enable running ansible-test on collections outside a collection root directory.

**Why it will work now:** Advancements in Python's pathlib and ansible-test's path handling now make this feature viable.

---

## Why It Died

The issue was abandoned due to technical limitations in ansible-test at the time, specifically its inability to run tests outside of a collection root or Ansible source directory.

## Why 2026 Changes Everything

Recent updates to ansible-test and advancements in Python's path handling make it feasible to support running tests outside of a collection root. Specifically, improvements in Python's pathlib and ansible-test's internal path management enable more flexible directory handling.

## Modern Architecture

To implement this feature, we can modify ansible-test to use Python's pathlib module for more flexible path resolution. The changes will involve updating the directory validation logic in ansible-test to allow for running tests from any directory, provided the collection or Ansible source is accessible. This will require adjustments to how ansible-test resolves the current working directory and collection paths.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | ansible, ansible-test, python |
| 💀 Year Abandoned | 2019 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
