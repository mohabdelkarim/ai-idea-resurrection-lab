# Proof of Concept: Index signature is missing in type (only on interfaces, not on type alias)

**Language:** typescript
**Estimated run time:** < 5 minutes

## Prerequisites

- Node.js 20+, npm install

## How to Run

```bash
npm install
npx ts-node main.ts
```

## What This Demonstrates

Enhance TypeScript to allow index signatures on interfaces for better type compatibility.
