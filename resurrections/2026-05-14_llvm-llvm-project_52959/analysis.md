# Analysis: Feature Request: Option to not check included files in clang-tidy

> Add an option to clang-tidy to skip analyzing included files for faster linting.

**Why it will work now:** This feature is now feasible due to improvements in Clang and LLVM tooling infrastructure.

---

## Why It Died

The issue didn't gain traction due to lack of a clear implementation plan and potential complexities in modifying clang-tidy's behavior.

## Why 2026 Changes Everything

With advancements in tools like LLVM 16 and the introduction of new features in Clang, a focused implementation could leverage modern C++ and improved plugin architectures.

## Modern Architecture

The feature could be implemented by introducing a new flag in clang-tidy, e.g., `--skip-included-files`, which would utilize the existing Clang tooling infrastructure. This would involve modifying the `clang-tidy` driver to skip analyzing included files when this flag is enabled. The implementation could follow the plugin pattern, allowing for easy extension and customization.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~80 hours |
| 🏷️ Tech Tags | clang-tidy, llvm, c++, linting |
| 💀 Year Abandoned | 2022 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
