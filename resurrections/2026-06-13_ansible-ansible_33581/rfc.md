# RFC: Name of task containing block is not printed to stdout

Summary
--------

This RFC proposes a solution to print the names of Ansible tasks containing blocks to stdout, enhancing the playbook output's informativeness. Currently, when a task contains a block, its name is not displayed, making it difficult for users to track the execution flow.

Motivation
----------

The primary motivation behind this proposal is to improve the visibility of task execution in Ansible playbooks. When tasks contain blocks, understanding the flow of execution is crucial for debugging and troubleshooting purposes. The absence of task names in the output hinders this understanding, leading to increased difficulty in managing complex playbooks.

Detailed Design
---------------

To implement this feature, we will modify the TaskExecutor class in the ansible/executor/task_executor.py module. The TaskExecutor is responsible for executing tasks, and it can be updated to include the block's parent task name in the output.

The proposed changes involve:

1.  Updating the TaskExecutor class to capture the task name when a block is executed.
2.  Utilizing Python's logging module to handle the output and ensure compatibility with existing logging configurations.
3.  Adding a new log message that includes the task name when a block is started.

The changes will be implemented in a backward-compatible manner to avoid disrupting existing workflows.

Drawbacks
---------

The potential drawbacks of this proposal are:

1.  Performance impact: The additional logging statement may introduce a minor performance overhead.
2.  Output clutter: The increased output may clutter the stdout, making it more challenging to parse.

However, these drawbacks can be mitigated by:

1.  Optimizing the logging statement to minimize performance impact.
2.  Providing an option to control the verbosity of the output.

Alternatives
------------

The alternative solutions considered are:

1.  Implementing a custom callback plugin to handle the output: This approach would require users to develop and maintain custom plugins, which may not be feasible for all use cases.
2.  Modifying the existing logging module: This approach may have broader implications for Ansible's logging system and may not be suitable for this specific use case.

Unresolved Questions
-------------------

The following questions remain unresolved:

1.  What is the optimal logging level for the task name output?
2.  Should an option be provided to control the verbosity of the output?
3.  How will this feature interact with existing logging configurations?

These questions will be addressed through further discussion and testing during the implementation phase.

---

*RFC generated by Resurrection Bot 🧬*
