# RFC: Node.js 15+ starts recommending promise based versions by default - Express 5.0 and wrapAsync

## 1. Summary

Express.js should add native support for promise-based APIs and async/await in routes to align with modern Node.js best practices. This RFC proposes a modern design for Express that integrates promise-based APIs and async/await support, potentially deprecating the `wrapAsync` function.

## 2. Motivation

Node.js 15+ has started recommending promise-based versions by default, and with the growing adoption of async/await, the need for native promise-based APIs in Express has become more pressing. The recent introduction of Node.js 20 and the upcoming EOL of Node.js 16 will drive more users to update to newer versions, making it a good time to revisit this issue. Additionally, the Express team has been actively working on Express 5.x, making it a good opportunity to integrate promise-based APIs.

## 3. Detailed Design

The proposed design involves creating a new set of promise-based APIs for Express, potentially using a facade pattern to maintain backward compatibility. The `wrapAsync` function could be deprecated in favor of native support for async/await in routes. The design would involve updating the Express router to handle promise-based middleware and routes, potentially using a decorator pattern to simplify the process.

The new design could include the following features:

*   Native support for async/await in routes
*   Promise-based APIs for Express
*   Deprecation of the `wrapAsync` function
*   Backward compatibility using a facade pattern

## 4. Drawbacks

The main drawback of this proposal is the potential disruption to existing applications that rely on the current implementation of Express. However, with the growing adoption of async/await and promise-based APIs in Node.js, it is likely that many applications will need to update to newer versions of Express anyway.

Other potential drawbacks include:

*   The need for additional testing and validation to ensure that the new design works correctly with existing applications
*   The potential for performance impacts due to the added overhead of promise-based APIs

## 5. Alternatives

One alternative to this proposal is to continue using the `wrapAsync` function and maintaining backward compatibility with the current implementation of Express. However, this approach would not align with modern Node.js best practices and would likely require additional maintenance and support.

Another alternative is to create a separate package or module that provides promise-based APIs for Express, rather than integrating them directly into the core Express package. However, this approach would likely require additional maintenance and support, and may not be as seamless for users.

## 6. Unresolved Questions

The following questions remain unresolved:

*   What is the best approach for maintaining backward compatibility with existing applications?
*   How will the new design impact performance, and what optimizations can be made to minimize any negative impacts?
*   What is the best way to handle errors and exceptions in promise-based APIs and async/await routes?

These questions will need to be addressed as the proposal moves forward and is implemented.

---

*RFC generated by Resurrection Bot 🧬*
