# RFC: Qwen-image support

# Summary

This RFC proposes the addition of Qwen-image support to ComfyUI, enhancing its AI image processing capabilities. The goal is to integrate the Qwen-image model, leveraging the Hugging Face Transformers library and PyTorch, to enable image-to-text functionality within ComfyUI's graph-based architecture.

## Motivation

ComfyUI currently lacks support for Qwen-image, a feature that could significantly enhance its AI image processing capabilities. The existence of the Qwen-Image model on Hugging Face and advancements in AI and ML libraries such as Transformers and PyTorch make it feasible to implement Qwen-image support. The growing demand for image-based AI models also increases the relevance of this feature.

## Detailed Design

The proposed design involves creating a new node in ComfyUI's graph-based architecture, specifically a 'QwenImageNode' that leverages the Hugging Face Transformers library for model inference. This node would handle image inputs and produce text outputs. The architecture would utilize a microservices-like approach for model serving, decoupling the node from the core ComfyUI engine.

The QwenImageNode would have the following properties:

- Input: Image
- Output: Text
- Model: Qwen-image (loaded via Hugging Face Transformers)

The node would be implemented using Python, with dependencies on PyTorch, Transformers, and Pillow for image processing.

## Drawbacks

The primary drawbacks of this proposal are:

- Additional dependency on Hugging Face Transformers and PyTorch
- Potential performance impact due to the added node and model serving
- Requirement for sufficient GPU resources to handle image processing

## Alternatives

Alternative approaches considered include:

- Using a different image-to-text model, such as BLIP or OFA
- Implementing a more lightweight image processing solution, such as using OpenCV
- Integrating Qwen-image support as a separate service, rather than a node within ComfyUI

However, leveraging the Qwen-image model with Hugging Face Transformers provides a well-maintained and widely-used solution, making it the preferred approach.

## Unresolved Questions

The following questions remain unresolved:

- How will the Qwen-image model be loaded and updated within ComfyUI?
- What are the specific performance requirements for running the Qwen-image node?
- How will the node be secured and authenticated within ComfyUI?

These questions will need to be addressed during implementation to ensure a seamless integration of Qwen-image support within ComfyUI.

---

*RFC generated by Resurrection Bot 🧬*
