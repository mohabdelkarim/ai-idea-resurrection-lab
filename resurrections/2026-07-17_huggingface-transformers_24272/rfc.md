# RFC: Finetuning Whisper with prompts

# Summary

This RFC proposes the implementation of finetuning for Whisper models using prompts in the Hugging Face Transformers library. The goal is to enable users to adapt Whisper models for specific transcription tasks by incorporating prompts into the finetuning process.

The proposed solution leverages the existing `Trainer` API and introduces a custom dataset class that includes prompts. The model's configuration will be adapted to accommodate prompt-based finetuning.

# Motivation

Whisper models have shown impressive performance in transcription tasks. However, their performance can be further improved by adapting them to specific domains or tasks. Finetuning Whisper models with prompts allows users to guide the model towards generating more accurate transcriptions for their specific use cases.

The demand for this feature has grown with the increasing adoption of Whisper models for various transcription tasks. Recent advancements in the Hugging Face Transformers library, particularly with the introduction of more flexible training APIs, make this feature more feasible.

# Detailed Design

The proposed solution involves the following steps:

1. Create a custom dataset class that includes prompts. This dataset class will be responsible for loading and preprocessing the data, including the prompts.

2. Modify the existing `Trainer` API to incorporate prompts into the model's forward pass. This can be achieved by adding a new argument to the `Trainer` class that allows users to specify the prompts.

3. Adapt the model's configuration to accommodate prompt-based finetuning. This may involve adding new configuration parameters that control the prompt-tuning process.

4. Implement the prompt-tuning logic in the model's forward pass. This will involve modifying the model's code to incorporate the prompts into the transcription generation process.

The solution will utilize existing techniques for prefix-tuning or prompt-based learning. The `Trainer` API will be extended to support prompt-based finetuning, and the custom dataset class will be used to load and preprocess the data.

# Drawbacks

The proposed solution has the following drawbacks:

1. Increased complexity: The introduction of prompts into the finetuning process adds complexity to the existing `Trainer` API and the model's configuration.

2. Additional computational resources: Finetuning a model with prompts may require additional computational resources, particularly if the prompts are complex or if a large number of prompts are used.

3. Potential for overfitting: The use of prompts may lead to overfitting, particularly if the prompts are not carefully designed or if the model is not regularized properly.

# Alternatives

The following alternatives were considered:

1. Implement a separate API for prompt-based finetuning: Instead of modifying the existing `Trainer` API, a separate API could be created for prompt-based finetuning. However, this approach may lead to duplicated code and increased maintenance overhead.

2. Use existing prompt-tuning libraries: There are existing libraries that provide prompt-tuning functionality. However, integrating these libraries into the Hugging Face Transformers library may require significant engineering effort and may not be compatible with the existing API.

# Unresolved Questions

The following questions remain unresolved:

1. How to design the prompts for finetuning Whisper models? What are the best practices for creating effective prompts?

2. How to regularize the model to prevent overfitting when using prompts?

3. What are the performance implications of finetuning Whisper models with prompts? Will the performance improvements be significant compared to other finetuning approaches?

These questions will need to be addressed through experimentation and testing once the implementation is complete.

---

*RFC generated by Resurrection Bot 🧬*
