# RFC: Support for Pydantic v2

## 1. Summary

This RFC proposes adding support for Pydantic v2 to the LangChain repository. The goal is to improve performance and compatibility with modern Python applications. We will introduce a new `PydanticV2Model` class, update the `LangChainModel` class to use it when Pydantic v2 is detected, and add a configuration option to allow users to opt-in to Pydantic v2 support.

## 2. Motivation

Pydantic v2 has been released with significant performance improvements, including 5-50x faster execution. Additionally, FastAPI continues to support Pydantic v2, making it a widely adopted and stable library. The LangChain community has likely grown, increasing the demand for Pydantic v2 support. By supporting Pydantic v2, we can provide better performance and compatibility for our users.

## 3. Detailed Design

To support Pydantic v2, we will:
* Introduce a new `PydanticV2Model` class that inherits from Pydantic's `BaseModel`.
* Use the `pydantic` library's built-in versioning features to handle differences between Pydantic v1 and v2.
* Update the `LangChainModel` class to use `PydanticV2Model` when Pydantic v2 is detected.
* Add a configuration option to allow users to opt-in to Pydantic v2 support.

We will use the following approach to detect Pydantic v2:
* Check the `pydantic` library version at runtime.
* If the version is 2 or higher, use `PydanticV2Model`.
* Otherwise, use the existing `LangChainModel`.

## 4. Drawbacks

The main drawback of this proposal is the potential breaking change for users who are currently using Pydantic v1. However, we will provide a configuration option to allow users to opt-in to Pydantic v2 support, and we will ensure that the existing `LangChainModel` class remains compatible with Pydantic v1.

## 5. Alternatives

An alternative approach would be to wait for Pydantic v2 to become the default version used in the LangChain repository. However, this approach may delay the benefits of Pydantic v2 for our users. Another alternative is to use a third-party library that provides compatibility between Pydantic v1 and v2, but this may add additional complexity and maintenance overhead.

## 6. Unresolved Questions

The following questions remain to be resolved:
* How will we handle the transition from Pydantic v1 to v2 for existing users?
* What are the potential performance implications of supporting Pydantic v2?
* How will we ensure that the `PydanticV2Model` class is thoroughly tested and validated?

---

*RFC generated by Resurrection Bot 🧬*
