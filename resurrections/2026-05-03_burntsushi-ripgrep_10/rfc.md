# RFC: add to package repositories

## Summary
The goal of this RFC is to propose a plan for adding ripgrep to package repositories, making it easily accessible to users of various Linux distributions.

## Motivation
ripgrep is a powerful search tool that can be useful for many developers and users. By adding it to package repositories, we can simplify the installation process and make it more widely available.

## Detailed Design
The plan involves creating a CI/CD pipeline that automates the packaging process for different distributions. This pipeline will utilize tools like GitHub Actions and cargo-deb to create Debian packages, and cargo-rpm for RPM packages. The pipeline will also handle uploads to the respective repositories.

## Drawbacks
One potential drawback is the maintenance burden of keeping the packages up-to-date. However, this can be mitigated by automating the packaging process and using scripts to simplify maintenance.

## Alternatives
An alternative approach would be to rely on users to compile and install ripgrep from source. However, this can be cumbersome and may deter some users from adopting the tool.

## Unresolved Questions
One unresolved question is how to handle dependencies and ensure that the packages are compatible with different versions of the distributions. This will require further research and testing to determine the best approach.

---

*RFC generated by Resurrection Bot 🧬*
