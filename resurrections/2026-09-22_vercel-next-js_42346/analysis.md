# Analysis: Next 13: router.push with different searchParams does not trigger suspense fallback

> router.push with new searchParams should trigger Suspense fallback in Next.js app directory.

**Why it will work now:** React 18.3 concurrent features and Next.js 13.5 server actions enable proper suspense handling.

---

## Why It Died

The issue stalled because Next.js lacked a stable API to link router.push searchParam changes with server‑side Suspense boundaries. At the time, the app router was still experimental and documentation was sparse, causing developers to avoid deep integration. Additionally, the core team prioritized other features, leaving limited bandwidth to address this edge case. Community contributions were scarce due to the complexity of coordinating server components and client navigation. Consequently, the bug remained unfixed and the discussion grew stale.

## Why 2026 Changes Everything

React 18.3 introduced refined concurrent rendering primitives that reliably suspend on pending data. Next.js 13.5 enhanced the App Router with explicit support for searchParam‑driven revalidation and integrated server actions. The new useSearchParams hook and improved fetch caching allow router.push to trigger server‑side data fetching within a Suspense boundary. These updates provide the missing plumbing to render a fallback when searchParams change. Together they resolve the technical gaps that blocked the original implementation.

## Modern Architecture

Implement the feature by wrapping the async server component in a React.Suspense boundary inside the app directory. Use Next.js's updated router API to listen for searchParam changes via the useSearchParams hook, and on each change invoke router.push with the new query. Trigger a revalidation by calling the new server action that invalidates the cache for the affected route, causing the server component to refetch data. The server component should export a fetch call that respects the Next.js fetch cache API, ensuring it suspends until data resolves. When the fetch suspends, the Suspense fallback (e.g., a skeleton) renders on the client while the server streams the new HTML once ready. This design leverages React's concurrent mode and Next.js's streaming SSR to provide seamless fallback transitions.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 8/10 |
| ⏱️ Effort Estimate | ~32 hours |
| 🏷️ Tech Tags | react, typescript |
| 💀 Year Abandoned | 2025 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
