# RFC: Support for right-aligned modules before line_break

# Summary

This RFC proposes the addition of support for right-aligning modules before line breaks in the Starship prompt. The goal is to enhance the customization options for users, allowing them to create more flexible and personalized prompts.




# Motivation

The Starship prompt is highly customizable, but it currently lacks the ability to right-align modules before line breaks. This limitation can make it difficult for users to create certain types of prompt layouts. By adding this feature, users will have more freedom to design their prompts according to their preferences.

The motivation behind this proposal is to address the growing demand for more advanced customization options in the Starship prompt. As the popularity of Starship continues to grow, users are seeking more complex and personalized prompt layouts. This feature will enable users to create prompts that are more visually appealing and better suited to their needs.




# Detailed Design

The proposed implementation involves adding an 'alignment' option to each module, which would allow users to specify 'left' or 'right' alignment. A 'line_break' module could trigger the right alignment of preceding modules. This would require changes to the prompt rendering engine to handle the new layout logic and to the configuration parser to support the new option.

The detailed design consists of the following components:

*   Add an 'alignment' option to each module, with possible values 'left' or 'right'.
*   Introduce a 'line_break' module that triggers right alignment of preceding modules.
*   Update the prompt rendering engine to handle the new layout logic.
*   Modify the configuration parser to support the new 'alignment' option.




# Drawbacks

The main drawback of this proposal is the added complexity to the prompt rendering engine and configuration parser. This may introduce potential performance overhead and increase the risk of bugs.

Other potential drawbacks include:

*   Increased complexity for users to configure and understand the new alignment option.
*   Potential inconsistencies in prompt rendering across different terminals and platforms.




# Alternatives

An alternative approach could be to introduce a new 'layout' option that allows users to specify custom layouts for their prompts. This would provide more flexibility but may also increase the complexity of the configuration options.

Another alternative is to use a more advanced templating engine to handle prompt rendering. This would allow for more complex and dynamic prompt layouts but may require significant changes to the Starship architecture.




# Unresolved Questions

The following questions remain unresolved:

*   How will the 'alignment' option interact with existing prompt customization options, such as 'format' and 'style'?
*   Will the 'line_break' module be able to handle multiple consecutive line breaks?
*   How will the prompt rendering engine handle right-aligned modules with varying widths?
*   What are the performance implications of this feature, and how will it affect the overall user experience?

These questions will need to be addressed during the implementation phase to ensure a seamless and efficient integration of the proposed feature.

---

*RFC generated by Resurrection Bot 🧬*
