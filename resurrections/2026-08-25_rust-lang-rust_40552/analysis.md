# Analysis: Enable --remap-path-prefix for absolute paths by default

> Add a rustc flag that remaps absolute source paths to relative crate‑based paths by default.

**Why it will work now:** Rust 1.54 stabilized the -C remap-path-prefix compiler option and enhanced source‑map infrastructure.

---

## Why It Died

The proposal lacked a clear consensus on the default behavior and required extensive changes to the compiler's path handling. At the time, the rustc flag infrastructure was immature, making integration risky. Limited community demand meant the effort was deprioritized in favor of more critical compiler features. Additionally, the absence of stable source‑map APIs hindered a clean implementation.

## Why 2026 Changes Everything

Rust 1.54 stabilized the -C remap-path-prefix compiler option, providing a formal flag mechanism. Subsequent releases improved the source‑map infrastructure, allowing reliable path transformations. LLVM now supports path remapping during debug info generation, reducing the need for custom workarounds. Cargo's reproducible build features make deterministic path handling practical. These advances collectively lower the technical barriers that existed in 2017.

## Modern Architecture

Introduce a new compiler flag --remap-path-prefix=default that activates the existing PathRemapper with a preset rule set. Extend the rustc::options module to parse and store this flag in the CrateConfig. Reuse the FilePathMapping structure in the source_map crate to apply the remapping during MIR and LLVM codegen phases. Implement a RemapStrategy enum to differentiate between absolute and relative paths, defaulting to crate‑relative mapping. Ensure the changes propagate to debuginfo generation via the LLVM DIBuilder integration. Add comprehensive tests in rustc/tests to validate privacy preservation and correct debugging information across platforms.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~70 hours |
| 🏷️ Tech Tags | rust, compiler |
| 💀 Year Abandoned | 2023 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
