# Analysis: Please expose current user and group ID as new variables for use inside docker-compose.yml

> Expose current user and group ID as variables in docker-compose.yml for convenient container user management.

**Why it will work now:** Recent Docker advancements and growing adoption of Docker Compose v2 enable secure implementation of user and group ID variables.

---

## Why It Died

The issue was not prioritized due to limited resources and potential security concerns. The proposed solution required adding new variables to docker-compose, which might introduce complexity and potential vulnerabilities.

## Why 2026 Changes Everything

Recent advancements in Docker and containerization have improved security and reduced the need for workarounds. Specifically, Docker's support for user namespace remapping and improved volume mounts has made it easier to manage user and group IDs. Additionally, the growing adoption of Docker Compose v2 has led to a renewed focus on feature requests.

## Modern Architecture

To implement this feature, we can leverage Docker's existing environment variable interpolation and add two new variables, COMPOSE_USER_ID and COMPOSE_GROUP_ID, which can be populated using the host system's user and group IDs. This can be achieved by modifying the docker-compose.yml parser to recognize these variables and replace them with the actual IDs. The implementation should ensure that these variables are only expanded when necessary, to avoid introducing security vulnerabilities.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | docker, docker-compose, go |
| 💀 Year Abandoned | 2020 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
