# Analysis: add `--build-arg` for `docker compose up --build`

> Add `--build-arg` support to `docker compose up --build` for streamlined image building.

**Why it will work now:** With BuildKit's stabilization and widespread adoption, integrating `--build-arg` into `docker compose up --build` is now viable.

---

## Why It Died

The issue was not prioritized due to the existing workaround of running `docker compose build` and `docker compose up` separately. The feature request, although acknowledged, did not gain enough traction or resources to implement.

## Why 2026 Changes Everything

With the advancements in Docker and Compose, particularly with the improved performance and capabilities of BuildKit, it's now feasible to integrate `--build-arg` directly into `docker compose up --build`.

## Modern Architecture

The implementation would involve modifying the `docker compose up` command to accept `--build-arg` and pass it to the underlying `docker build` command. This could be achieved by leveraging the existing BuildKit APIs and integrating them into the Compose codebase. The design would focus on maintaining a seamless user experience while extending the functionality of the `up` command.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 5/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | docker, compose, buildkit |
| 💀 Year Abandoned | 2023 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
