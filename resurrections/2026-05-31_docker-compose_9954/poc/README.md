# Proof of Concept: add `--build-arg` for `docker compose up --build`

**Language:** go
**Estimated run time:** < 5 minutes

## Prerequisites

- Go 1.22+

## How to Run

```bash
go run main.go
```

## What This Demonstrates

Add `--build-arg` support to `docker compose up --build` for streamlined image building.
