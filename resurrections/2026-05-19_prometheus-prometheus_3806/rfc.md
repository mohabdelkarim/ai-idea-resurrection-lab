# RFC: Proposal for improving rate/increase 

## 1. Summary

This RFC proposes improvements to the `rate()` and `increase()` functions in Prometheus to provide more accurate and flexible calculations. The goal is to enhance the existing functions to better handle various use cases and provide more precise results.

## 2. Motivation

The current implementation of `rate()` and `increase()` functions in Prometheus has limitations. The `rate()` function calculates the per-second average rate of increase of the time series in the range vector. However, it does not account for the scrape interval and evaluation interval, which can lead to inaccurate results. The `increase()` function calculates the increase of the time series in the range vector, but it also has limitations, such as not considering the last point before the range.

The growing need for more accurate and flexible rate and increase calculations in modern monitoring and observability workflows drives the motivation for this proposal. New tools and APIs have emerged that could make the proposed changes more feasible, such as improved time series data handling libraries and more efficient query engines.

## 3. Detailed Design

The proposed design involves introducing a new function, `rate_over_time()`, which takes into account the scrape interval and evaluation interval. This function would provide a more flexible time range definition, including the last point before the range.

The `rate_over_time()` function would accept a range vector and a time range as input and return the rate of increase over the specified time range. The function would consider the scrape interval and evaluation interval to ensure accurate calculations.

The design would also involve optimizing the query engine to handle the increased data volume. This could be achieved by leveraging a column-store database or other efficient data processing frameworks.

## 4. Drawbacks

The proposed changes have some drawbacks:

*   Backward compatibility: The introduction of a new function, `rate_over_time()`, might break existing queries that rely on the current `rate()` and `increase()` functions.
*   Performance impact: The optimized query engine and new function might introduce additional performance overhead, potentially affecting the overall performance of Prometheus.

## 5. Alternatives

Some alternative approaches could be:

*   Enhance the existing `rate()` and `increase()` functions to account for the scrape interval and evaluation interval. However, this might be challenging without breaking backward compatibility.
*   Introduce a new function that provides a more flexible time range definition, but does not consider the scrape interval and evaluation interval.

## 6. Unresolved Questions

Some questions remain unresolved:

*   How to ensure backward compatibility with existing queries that rely on the current `rate()` and `increase()` functions?
*   What is the expected performance impact of the optimized query engine and new function?
*   How to handle edge cases, such as irregular scrape intervals or evaluation intervals?

Further discussion and experimentation are necessary to address these questions and refine the proposal.

---

*RFC generated by Resurrection Bot 🧬*
