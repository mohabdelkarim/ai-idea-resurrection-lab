# RFC: Support running ansible-test on collections outside a collection root

## 1. Summary

This RFC proposes to enhance the `ansible-test` command to support running on collections outside of a collection root directory. The goal is to provide more flexibility for collection maintainers and contributors by allowing them to run tests on their collections without being constrained by a specific directory structure.

## 2. Motivation

Currently, `ansible-test` requires that collections be located within a collection root directory. This limitation can be inconvenient for collection maintainers and contributors who may need to test their collections in different contexts or locations. By supporting running `ansible-test` on collections outside of a collection root directory, we can improve the overall user experience and make it easier to test and validate collections.

## 3. Detailed Design

To implement this feature, we propose the following changes:

* Introduce a new command-line option `--collection-root` for `ansible-test`, which allows users to specify the root directory of the collection.
* Add a new configuration option `collection_root` to `ansible.cfg` to enable this feature.
* Update the `ansible-test` code to handle the new option and configuration, and to correctly resolve the collection's root directory.
* Add tests to ensure the feature works correctly and handles different scenarios.

The implementation will involve modifying the `ansible-test` command to accept the `--collection-root` option and updating the code to handle this option. We will also need to update the documentation to reflect the new option and configuration.

## 4. Drawbacks

The main drawback of this feature is that it may introduce additional complexity to the `ansible-test` command and configuration. However, the benefits of increased flexibility and convenience for collection maintainers and contributors outweigh the potential drawbacks.

## 5. Alternatives

One alternative to this approach is to require collection maintainers and contributors to restructure their collections to conform to the current collection root directory requirement. However, this approach is not feasible as it would be too restrictive and inconvenient for users.

Another alternative is to use a different testing framework that does not have this limitation. However, this approach is not viable as `ansible-test` is a core part of the Ansible ecosystem and is widely used by collection maintainers and contributors.

## 6. Unresolved Questions

The following questions remain to be addressed:

* How will the `--collection-root` option interact with other `ansible-test` options and configurations?
* How will we handle cases where the collection root directory is not specified or is invalid?
* How will we ensure that the feature works correctly with different collection structures and layouts?

These questions will need to be addressed during the implementation phase to ensure that the feature is correctly implemented and works as expected.

---

*RFC generated by Resurrection Bot 🧬*
