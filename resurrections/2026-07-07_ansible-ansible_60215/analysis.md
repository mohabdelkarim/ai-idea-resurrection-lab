# Analysis: Support running ansible-test on collections outside a collection root

> Support running ansible-test on collections outside a collection root directory.

**Why it will work now:** Ansible's testing framework now supports flexible collection handling.

---

## Why It Died

The issue was abandoned due to the complexity of implementing a feature that would allow ansible-test to run outside of a collection root directory. The current implementation required a specific directory structure to function correctly, which was a limitation for collection maintainers and contributors. The issue was not addressed at the time due to the lack of a clear solution.

## Why 2026 Changes Everything

With the advancements in Ansible's testing framework and the introduction of new features such as ansible-test's ability to handle collections more flexibly, it is now feasible to support running ansible-test on collections outside a collection root. The recent changes in Ansible's architecture and the growth of the collection ecosystem have made this feature more viable.

## Modern Architecture

To implement this feature, we can modify the ansible-test command to accept a --collection-root option, allowing users to specify the root directory of the collection. We can also introduce a new configuration option to ansible.cfg to enable this feature. The implementation would involve updating the ansible-test code to handle the new option and configuration, as well as adding tests to ensure the feature works correctly.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | ansible, ansible-test, collections |
| 💀 Year Abandoned | 2019 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
