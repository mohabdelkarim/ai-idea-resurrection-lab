# Proof of Concept: Support running ansible-test on collections outside a collection root

**Language:** python
**Estimated run time:** < 5 minutes

## Prerequisites

- Python 3.12+, pip install requirements

## How to Run

```bash
pip install -r requirements.txt
python main.py
```

## What This Demonstrates

Support running ansible-test on collections outside a collection root directory.
