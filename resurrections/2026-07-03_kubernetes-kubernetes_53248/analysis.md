# Analysis: kubectl provides a way to find which RoleBinding/ClusterRoleBinding is related to a serviceAccount

> kubectl should provide a command to find RoleBinding/ClusterRoleBinding related to a serviceAccount.

**Why it will work now:** The kubectl plugin system and improved Go libraries make this feature feasible in 2026.

---

## Why It Died

The issue was abandoned due to lack of evidence and prioritization. At the time, there were no clear requirements or use cases for this feature, and implementing it would have required significant development effort.

## Why 2026 Changes Everything

With the increasing adoption of service accounts and role bindings in Kubernetes, the need for a simple command-line tool to find related role bindings has become more pressing. The kubectl plugin system and improved Go libraries for Kubernetes API interactions make this feature more feasible. Additionally, the growing importance of security and access control in Kubernetes clusters highlights the value of this feature.

## Modern Architecture

The feature could be implemented as a kubectl subcommand, leveraging the existing Kubernetes Go libraries for API interactions. The command would take a service account name and namespace as input and query the Kubernetes API for related role bindings. The implementation would involve creating a new Go package for the kubectl plugin, adding API calls to retrieve role bindings, and integrating the plugin with the kubectl command-line interface.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | kubernetes, kubectl, rbac |
| 💀 Year Abandoned | 2017 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
