# Proof of Concept: Increase verbosity on a per task basis

**Language:** python
**Estimated run time:** < 5 minutes

## Prerequisites

- Python 3.12+, pip install requirements

## How to Run

```bash
pip install -r requirements.txt
python main.py
```

## What This Demonstrates

Increase verbosity on a per-task basis in Ansible playbooks.
