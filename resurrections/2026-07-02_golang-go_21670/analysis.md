# Analysis: proposal: Go 2: Have functions auto-implement interfaces with only a single method of that same signature

> Automatically implement single-method interfaces for functions with matching signatures in Go.

**Why it will work now:** The Go language's type system has evolved with the introduction of generics and the 'any' type, making this feature more.

---

## Why It Died

The proposal was likely abandoned due to concerns about backwards compatibility and the potential for subtle bugs in existing code. The Go team may have also been hesitant to introduce a new language feature that could change the behavior of existing code. Additionally, the proposal may have been seen as not aligning with the Go language's design principles.

## Why 2026 Changes Everything

With the introduction of Go 1.19's generics and the upcoming Go 1.20's improved error handling, the language is becoming more expressive and flexible. The recent introduction of the 'any' type and the improvements to the type system may also make it easier to implement this feature. Furthermore, the growing popularity of Go and the increasing demand for more concise and expressive code may make this feature more appealing.

## Modern Architecture

To implement this feature, we could modify the Go compiler to automatically generate an interface implementation for a function if it matches the signature of a single-method interface. This could be achieved by adding a new AST node type to represent the interface implementation and then modifying the type checker to generate the implementation. We would also need to update the documentation and add tests to ensure that the feature works correctly.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~120 hours |
| 🏷️ Tech Tags | go, golang, interface |
| 💀 Year Abandoned | 2017 |
| 🔬 Has PoC | No |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
