# RFC: Intermediate variables (OR: add interpolation support to input variables)

## Summary
This RFC proposes the introduction of intermediate variables in Terraform, which can be used to store complex expressions that involve other input variables. The proposed feature aims to provide a more flexible and dynamic way of handling variables in Terraform configurations.

## Motivation
The current Terraform configuration language lacks support for intermediate variables, making it difficult for users to define complex input variable defaults. The proposed feature aims to address this limitation and provide a more robust and flexible variable system in Terraform.

## Detailed Design
The proposed feature introduces a new `intermediate` keyword that allows users to define temporary variables within a Terraform configuration. These intermediate variables can be used to store complex expressions that involve other input variables, and can be referenced in the configuration using a new `intermediate.` prefix. The feature also includes support for auto-completion and diagnostics for intermediate variables in the VS Code Terraform extension.

## Drawbacks
One potential drawback of the proposed feature is the added complexity it introduces to the Terraform configuration language. The feature may also require significant changes to the Terraform state management system, which could be challenging to implement.

## Alternatives
One alternative to the proposed feature is to use external tools or scripts to manage complex variable expressions. However, this approach may not be as seamless or integrated as the proposed feature, and may require additional maintenance and upkeep.

## Unresolved Questions
One unresolved question is how to handle the evaluation of intermediate variables in the context of Terraform's state management system. Another unresolved question is how to provide adequate support for auto-completion and diagnostics for intermediate variables in the VS Code Terraform extension.

---

*RFC generated by Resurrection Bot 🧬*
