# Analysis: Intermediate variables (OR: add interpolation support to input variables)

> The proposed feature introduces intermediate variables in Terraform, allowing users to define complex input variable defaults and providing a more flexible and dynamic way of...

**Why it will work now:** The proposed feature will succeed because it addresses a long-standing limitation in the Terraform configuration language, providing a more robust and flexible variable system that...

---

## Why It Died

The Terraform configuration language lacked support for intermediate variables, making it difficult for users to define complex input variable defaults. The VS Code Terraform extension API also lacked features like auto-completion and diagnostics for Terraform configurations, which made it harder for users to work with complex Terraform configurations. Additionally, the Terraform state management system was not designed to handle complex variable interpolation, which limited the ability to implement intermediate variables. The lack of a clear and consistent way to define and use intermediate variables in Terraform configurations also contributed to the issue's abandonment. Furthermore, the Terraform team's focus on other features and priorities at the time may have diverted attention away from this issue.

## Why 2026 Changes Everything

The recent advancements in Terraform 1.4, including improved support for variable validation and the introduction of the `terraform console` command, provide a solid foundation for implementing intermediate variables. The growing adoption of Terraform as a core infrastructure-as-code tool also increases the demand for more advanced features like intermediate variables. Moreover, the development of new Terraform providers and modules has created a need for more flexible and dynamic variable handling. The rise of cloud-based infrastructure and the need for more efficient and automated deployment processes also make intermediate variables a crucial feature for Terraform users. Additionally, the increasing popularity of Terraform CDK, which allows users to define infrastructure in programming languages, highlights the importance of having a robust and flexible variable system in Terraform.

## Modern Architecture

To implement intermediate variables in Terraform, we can introduce a new `intermediate` keyword that allows users to define temporary variables within a Terraform configuration. These intermediate variables can be used to store complex expressions that involve other input variables, and can be referenced in the configuration using a new `intermediate.` prefix. We can also add support for auto-completion and diagnostics for intermediate variables in the VS Code Terraform extension. To handle the state management for intermediate variables, we can extend the Terraform state management system to include a new `intermediate` section that stores the values of intermediate variables. We can use a combination of Terraform's built-in functions and external data sources to evaluate the expressions used in intermediate variables. The `intermediate` keyword can be used in conjunction with other Terraform keywords, such as `input` and `output`, to create complex variable expressions. We can also add support for referencing input values of a module using a new `module.` prefix, which can be used in conjunction with intermediate variables to create more flexible and dynamic configurations.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 8/10 |
| ⏱️ Effort Estimate | ~120 hours |
| 🏷️ Tech Tags | terraform, infrastructure-as-code, variable-management, configuration-language |
| 💀 Year Abandoned | 2017 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
