# Analysis: Add an option similar to -o, --only-matching

> Add a new option to ripgrep to print only the matched parts of a file.

**Why it will work now:** This feature is now feasible due to improvements in the regex crate and modern Rust ecosystem.

---

## Why It Died

The issue was abandoned due to lack of discussion and unclear requirements. The original feature request was not prioritized, and no one took ownership of implementing it. The ripgrep project was actively maintained at the time, but this specific feature was not pursued. The issue was filed in 2016 and last updated in 2017.

## Why 2026 Changes Everything

With the current state of Rust and the ecosystem, implementing this feature is more feasible. The regex crate, which ripgrep relies on, has improved significantly since 2016. Additionally, the use of modern Rust features and libraries like clap for argument parsing can simplify the implementation.

## Modern Architecture

The feature can be implemented by adding a new flag to the clap argument parser. When this flag is enabled, the matcher will yield only the matched parts of the line, instead of the entire line. This can be achieved by modifying the existing matcher logic to return a subset of the match. The `regex` crate's `Match` struct can be used to extract the matched text.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | ripgrep, regex, clap, rust |
| 💀 Year Abandoned | 2017 |
| 🔬 Has PoC | No |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
