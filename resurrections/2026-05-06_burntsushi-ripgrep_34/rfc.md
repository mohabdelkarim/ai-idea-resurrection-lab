# RFC: Add an option similar to -o, --only-matching

## Summary

This RFC proposes the addition of a new option to ripgrep, similar to the existing `-o` or `--only-matching` flag found in GNU grep. The proposed flag, `--matched`, will instruct ripgrep to print only the matched parts of a file, rather than the entire line. This feature will enhance the usability of ripgrep for users who need to extract specific matched text from files.

## Motivation

The motivation behind this feature is to provide users with more flexibility in processing and extracting matched text. Currently, ripgrep prints the entire line that contains a match, which can be overwhelming when dealing with large files or when the matched text is a small part of a long line. By adding the `--matched` flag, users will be able to focus on the exact text that matched the pattern, making it easier to analyze and process the results.

## Detailed Design

The `--matched` flag will be implemented using the clap argument parser. When this flag is enabled, the matcher logic will be modified to yield only the matched parts of the line. This can be achieved by leveraging the `regex` crate's `Match` struct to extract the matched text. The implementation will involve the following steps:

* Add a new flag to the clap argument parser with the name `--matched` and a short alias `-m`.
* Modify the matcher logic to check for the presence of the `--matched` flag.
* When the flag is enabled, use the `regex` crate's `Match` struct to extract the matched text from the input line.
* Print only the matched text to the output.

## Drawbacks

One potential drawback of this feature is that it may change the output format of ripgrep in a way that breaks existing scripts or workflows that rely on the current output. However, this can be mitigated by documenting the new flag and its behavior clearly, and by providing examples of how to use it. Another potential drawback is that extracting the matched text may incur additional performance overhead, but this is expected to be minimal given the optimized implementation of the `regex` crate.

## Alternatives

An alternative approach to implementing the `--matched` flag would be to use an existing flag, such as `-o` or `--only-matching`, which is already available in GNU grep. However, this flag has slightly different behavior than what is proposed here, and it may not be desirable to replicate its behavior exactly. Another alternative would be to implement a more general-purpose feature that allows users to specify a custom output format, but this would likely be more complex and harder to use.

## Unresolved Questions

One unresolved question is how to handle cases where the input file contains multiple matches on the same line. Should the `--matched` flag print each match on a separate line, or should it print all matches on the same line, separated by some delimiter? Another unresolved question is how to handle cases where the matched text is empty (e.g., when the pattern matches an empty string). Should the `--matched` flag print an empty line in this case, or should it skip printing anything at all?

---

*RFC generated by Resurrection Bot 🧬*
