# RFC: Support for GitHub Enterprise

## Summary

This RFC proposes the addition of support for GitHub Enterprise Server (GHES) to the GitHub CLI repository. The goal is to enhance the usability of the CLI for enterprise users by allowing them to interact with their GHES instances seamlessly.

## Motivation

The GitHub CLI currently lacks support for GitHub Enterprise Server, which limits its adoption in enterprise environments. Many organizations use GHES for their development and collaboration needs, and the absence of CLI support hinders their ability to fully leverage the GitHub platform. The complexity of implementing compatible API endpoints for various GHES versions was a significant barrier to implementing this feature in the past. However, recent advancements in GitHub's API, including the introduction of the GraphQL API and improved support for GHES, have made this endeavor more feasible.

## Detailed Design

The implementation of GHES support will involve the following key components:

*   **GitHubEnterpriseClient Class**: A new class, `GitHubEnterpriseClient`, will be introduced to handle API requests to GHES instances. This class will leverage existing GitHub API client libraries, such as the Go client library, to interact with GHES APIs.
*   **Service Discovery and Configuration Management**: The system will incorporate service discovery and configuration management to handle multiple GHES instances. This will enable users to easily configure and connect to their GHES instances.
*   **API Endpoint Compatibility**: The implementation will ensure compatibility with various GHES versions by utilizing the GitHub API client libraries and adhering to the GitHub API documentation.

## Drawbacks

The primary drawbacks of implementing GHES support are:

*   **Increased Complexity**: Adding support for GHES will introduce additional complexity to the GitHub CLI codebase, which may lead to increased maintenance efforts.
*   **Testing and Validation**: Ensuring compatibility with multiple GHES versions will require extensive testing and validation efforts.

## Alternatives

Alternative approaches to implementing GHES support include:

*   **Using a Third-Party Library**: Utilizing a third-party library that provides GHES support could accelerate the implementation process. However, this approach may introduce additional dependencies and potential security risks.
*   **Implementing a Separate CLI Tool**: Developing a separate CLI tool specifically for GHES users could provide a more focused solution. However, this approach may lead to duplicated effort and a fragmented user experience.

## Unresolved Questions

The following questions remain to be addressed:

*   **GHES Version Support**: Which versions of GHES will be supported, and how will the implementation ensure compatibility across different versions?
*   **Authentication and Authorization**: How will authentication and authorization be handled for GHES instances, and what mechanisms will be put in place to ensure secure interactions with GHES APIs?
*   **Testing and Quality Assurance**: What testing and quality assurance processes will be implemented to ensure the reliability and stability of the GHES support feature?

---

*RFC generated by Resurrection Bot 🧬*
