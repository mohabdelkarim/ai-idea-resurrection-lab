# Analysis: Support for multiple ports in annotations

> Prometheus enhancement to support multiple ports in annotations for more flexible service discovery.

**Why it will work now:** The Kubernetes ecosystem evolution and increased demand for multi-port scraping make this feature viable now.

---

## Why It Died

The issue was abandoned likely due to the complexity of implementing support for multiple ports in annotations and the potential impact on the existing service discovery mechanism. At the time, Prometheus's service discovery and scraping mechanisms were not designed to handle multiple ports per target easily. The community might have been focused on other priorities, and the feature might have seemed too niche or complex to implement.

## Why 2026 Changes Everything

With the advancements in Kubernetes and container orchestration, the need for scraping multiple ports from pods or containers has become more common. The Prometheus community has continued to evolve, and new features have been added to improve service discovery and configuration. Specifically, the introduction of more flexible service discovery mechanisms and improvements in the Prometheus configuration could now make it feasible to support multiple ports in annotations.

## Modern Architecture

To implement this feature, we could extend the existing annotation parsing logic to support multiple ports specified in a single annotation, potentially using a comma-separated list or a more structured format like JSON. The Prometheus server would need to be updated to handle these new annotations, possibly by introducing a new configuration option to enable or disable this feature. Additionally, the Kubernetes service discovery component would need to be updated to properly handle and expose these multiple ports. The implementation would involve changes to the annotation parsing, service discovery logic, and potentially the scraping mechanism.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~60 hours |
| 🏷️ Tech Tags | prometheus, service discovery, kubernetes |
| 💀 Year Abandoned | 2018 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
