# Analysis: [fmt] Option to remove semicolons

> Add an option to deno fmt to remove semicolons from JavaScript and TypeScript code.

**Why it will work now:** This feature succeeds now because deno fmt needs customizable formatting options to match the diverse style conventions in the JavaScript.

---

## Why It Died

The issue was not prioritized and lost traction due to the maintainers' focus on other features. Deno's formatting tool, deno fmt, was still in its early stages, and the team might have been hesitant to add a new option that could potentially conflict with existing style conventions. Additionally, the issue did not receive enough engagement from the community to keep it alive.

## Why 2026 Changes Everything

With the growth of the Deno community and the increasing adoption of JavaScript and TypeScript, the need for customizable formatting options has become more pressing. The existence of tools like Prettier and ESLint, which offer flexible formatting and linting capabilities, has set a new standard for developer tooling. Specifically, the TypeScript API and dprint's plugin architecture have matured, making it easier to implement this feature.

## Modern Architecture

To implement this feature, we can introduce a new option `useSemicolon` in the deno fmt configuration, which defaults to `true`. When set to `false`, it will utilize dprint's `asi` option to remove automatically insertable semicolons. This design follows the existing pattern of formatting options in deno fmt, such as `useTabs` and `indentWidth`. The `useSemicolon` option will be exposed through the deno fmt CLI and configuration file.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~24 hours |
| 🏷️ Tech Tags | deno, typescript, javascript, code-formatting |
| 💀 Year Abandoned | 2023 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
