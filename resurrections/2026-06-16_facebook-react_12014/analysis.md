# Analysis: Have Fragments support dangerouslySetInnerHTML

> Enable dangerouslySetInnerHTML on React Fragments for flexible HTML injection.

**Why it will work now:** React Server Components and modern sanitization libraries like DOMPurify now make this feature feasible.

---

## Why It Died

The issue was abandoned likely due to the complexity of implementing dangerouslySetInnerHTML on Fragments, which are meant to be lightweight and not render as a DOM node. The React team may have been concerned about the security implications of allowing arbitrary HTML injection, even with proper escaping.

## Why 2026 Changes Everything

With the advancements in React's rendering engine and the introduction of new features like React Server Components, the technical landscape has changed. Modern tooling and libraries like DOMPurify for sanitizing HTML have matured, making it safer to implement this feature. Additionally, the growing use of server-side rendering and static site generation with React has increased the demand for flexible HTML injection methods.

## Modern Architecture

To implement this feature, we could add a check for dangerouslySetInnerHTML on Fragment components. If present, React would need to unwrap the Fragment and directly inject the HTML into the DOM, bypassing the normal Fragment rendering logic. This could involve modifications to React's reconciliation algorithm and the Fragment component's implementation. We would also need to ensure proper escaping and sanitization of the injected HTML to prevent security vulnerabilities.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 8/10 |
| ⏱️ Effort Estimate | ~80 hours |
| 🏷️ Tech Tags | react, typescript, ssr |
| 💀 Year Abandoned | 2018 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
