# RFC: Feature Request: XTRIM option that deletes all messages already acknowledged by all groups

## 1. Summary

This RFC proposes the implementation of an XTRIM option for Redis streams that deletes all messages already acknowledged by all consumer groups. The new option will enhance the efficiency and usability of Redis as a message broker by allowing users to automatically remove acknowledged messages, thereby optimizing storage and performance.

## 2. Motivation

Redis streams have become a popular choice for message queuing and event-driven architectures. However, as the usage of Redis streams grows, so does the need for efficient message management. Currently, users have to manually track and remove acknowledged messages, which can be cumbersome and error-prone. The proposed XTRIM option will simplify this process, making Redis an even more attractive solution for message brokers.

The main motivations are:
- Simplify message management for Redis stream users.
- Improve performance by reducing the storage footprint of acknowledged messages.
- Enhance the overall usability of Redis as a message broker.

## 3. Detailed Design

The proposed XTRIM option will be implemented as part of the existing XTRIM command. The new option will be called 'ACK' or 'acknowledged', which will specify that only messages acknowledged by all consumer groups should be trimmed.

Here is a high-level overview of the design:
- Modify the XTRIM command to accept an 'ACK' option.
- When the 'ACK' option is specified, Redis will identify all messages in the stream that have been acknowledged by all consumer groups.
- Redis will then remove these acknowledged messages from the stream.

The implementation details are as follows:
- Utilize Redis's existing stream data structures and consumer group APIs.
- Leverage Redis's internal stream trimming logic to efficiently remove acknowledged messages.
- Ensure consistency across multiple consumer groups.
- Handle edge cases like partially acknowledged messages.

## 4. Drawbacks

The main drawbacks of this proposal are:
- Potential performance impact: trimming acknowledged messages could introduce additional latency or CPU usage.
- Complexity: implementing the 'ACK' option will require a deep understanding of Redis's internal stream data structures and consumer group APIs.

However, these drawbacks can be mitigated by:
- Conducting thorough performance testing to ensure the implementation does not significantly impact Redis's performance.
- Implementing the 'ACK' option in a way that minimizes complexity and maximizes maintainability.

## 5. Alternatives

One alternative to the proposed XTRIM option is to use a Lua script to manually track and remove acknowledged messages. However, this approach has several drawbacks:
- It requires users to manually implement and maintain the Lua script.
- It may not be as efficient as a native implementation.

Another alternative is to use a third-party library or tool to manage Redis streams. However, this approach may introduce additional dependencies and complexity.

## 6. Unresolved Questions

The following questions remain unresolved:
- How will the 'ACK' option interact with existing stream trimming options, such as 'MAXLEN' and 'MINID'?
- How will the 'ACK' option handle edge cases like partially acknowledged messages or messages with multiple consumer groups?
- What are the performance implications of implementing the 'ACK' option, and how can they be mitigated?

These questions will need to be addressed during the implementation phase to ensure the 'ACK' option is correctly implemented and functions as expected.

---

*RFC generated by Resurrection Bot 🧬*
