# Analysis: Feature Request: XTRIM option that deletes all messages already acknowledged by all groups

> Implement an XTRIM option to delete acknowledged messages in Redis streams.

**Why it will work now:** Redis 7.0's stream optimizations enable efficient XTRIM implementation.

---

## Why It Died

The issue was abandoned due to lack of a clear technical path forward at the time. The feature request required a deep understanding of Redis's internal workings and the streaming data structures. The original proposer attempted a Lua script solution, but worried about its efficiency.

## Why 2026 Changes Everything

Recent advancements in Redis, such as improved support for streams and consumer groups, make this feature more feasible. Specifically, Redis 7.0's new stream capabilities and optimizations provide a foundation for efficient trimming. Additionally, the growing adoption of Redis as a message broker increases the demand for robust stream management features.

## Modern Architecture

The proposed XTRIM option would utilize Redis's existing stream data structures and consumer group APIs to efficiently delete acknowledged messages. This could involve modifying the XTRIM command to accept a new option, which would then leverage Redis's internal stream trimming logic. The implementation would need to ensure consistency across multiple consumer groups and handle edge cases like partially acknowledged messages.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 4/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | redis, streams, consumer-groups |
| 💀 Year Abandoned | 2019 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
