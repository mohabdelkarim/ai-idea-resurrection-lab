# Proof of Concept: Add a (singular) `$prop` rune

**Language:** typescript
**Estimated run time:** < 5 minutes

## Prerequisites

- Node.js 20+, npm install

## How to Run

```bash
npm install
npx ts-node main.ts
```

## What This Demonstrates

Introduce a singular `$prop` rune for concise prop declarations with types in Svelte.
