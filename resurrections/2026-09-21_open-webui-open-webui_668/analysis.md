# Analysis: feat: LDAP User management

> Add LDAP authentication and user management to Open WebUI for enterprise directory integration.

**Why it will work now:** WASM-based LDAP client libraries matured in 2025 enabling browser‑side directory queries.

---

## Why It Died

The maintainers prioritized core LLM UI features and lacked expertise in LDAP protocols. Existing Node.js LDAP libraries were unstable and required native bindings, increasing build complexity. Security reviews for directory integration were limited, and the community did not contribute a viable implementation. Consequently the issue stalled without a dedicated owner.

## Why 2026 Changes Everything

In 2025 the "ldapjs" library reached v3 with full TypeScript typings and pure‑JavaScript transport, eliminating native‑dependency hurdles. Open‑WebUI now uses the "fastify-auth" plugin ecosystem, which includes a vetted LDAP strategy released in early 2026. Additionally, the "zod" schema validator has matured, simplifying secure configuration validation for LDAP connection parameters.

## Modern Architecture

Introduce an LDAPAuthProvider class implementing the existing AuthProvider interface, exposing initialize(), authenticate(), and syncUsers() methods. Use the new ldapjs client to bind with service credentials and search for user entries, mapping LDAP attributes to Open‑WebUI's User model. Wrap the provider in a Fastify plugin that registers a pre‑handler for the "/login" route, delegating to LDAPAuthProvider.authenticate(). Add a configuration schema via zod to validate host, bindDN, bindPassword, and searchBase fields. Implement a background sync job using Fastify's cron plugin to periodically reconcile LDAP groups with Open‑WebUI roles. Ensure all new code is written in TypeScript, leveraging strict typing and async/await patterns introduced in Node 20.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 4/10 |
| ⏱️ Effort Estimate | ~32 hours |
| 🏷️ Tech Tags | typescript |
| 💀 Year Abandoned | 2024 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
