# RFC: terraform get: can't use variable in module source parameter?

## Summary
This RFC proposes a new feature for Terraform that allows users to use variables in the source parameter of a module. This feature would provide users with more flexibility and control over their infrastructure provisioning processes.

## Motivation
The motivation behind this feature is to provide users with a way to dynamically configure module sources and dependencies. This would allow users to create more modular and flexible Terraform configurations that can be easily managed and updated.

## Detailed Design
The detailed design for this feature would involve leveraging Terraform's built-in support for variables and modules to create a flexible and dynamic module management system. This would involve using Terraform's module registry to manage and version modules, and using variables to configure module sources and dependencies. The design would also involve creating a custom Terraform provider to handle the dynamic module sourcing and dependency management.

## Drawbacks
The main drawback of this feature is that it would require significant changes to Terraform's core architecture and module management system. This could potentially introduce new bugs and stability issues.

## Alternatives
One alternative to this feature would be to use Terraform's built-in support for modules and variables to create a custom module management system. This would involve creating a set of custom modules and variables that can be used to manage and configure module sources and dependencies.

## Unresolved Questions
One unresolved question is how to handle module dependencies and versioning in a dynamic module management system. This would require significant changes to Terraform's module registry and versioning system.

---

*RFC generated by Resurrection Bot 🧬*
