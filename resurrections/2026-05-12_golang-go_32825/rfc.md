# RFC: proposal: leave "if err != nil" alone?

### 1. Summary

This RFC proposes revisiting the syntax for error handling in Go, specifically the 'if err != nil' construct. With the evolution of the language and the introduction of new features such as error wrapping and improved error messages, there is an opportunity to simplify error handling syntax while maintaining backwards compatibility.

The goal is to reduce boilerplate code and improve readability, drawing inspiration from modern design principles and existing patterns in other languages.

### 2. Motivation

The current 'if err != nil' syntax, while straightforward, leads to verbose code and boilerplate. As Go projects grow in complexity, efficient error handling becomes crucial. The introduction of error wrapping in Go 1.20 and enhanced error messages in Go 1.21 provides a foundation for reevaluating error handling.

Many developers are accustomed to the existing pattern, but with the growing popularity of Go, there's a pressing need for more efficient mechanisms. This RFC aims to address this need by proposing a modern design approach.

### 3. Detailed Design

The proposed design involves introducing a more concise syntax for error handling, potentially through a new keyword or function that handles errors in a centralized way. This could involve:

- Introducing a new function, `try`, that wraps the result of a function call and returns an error, allowing for more concise error handling.
- Utilizing existing error wrapping and unwrapping mechanisms to streamline error handling.

Example:

go
result, err := try.FuncThatReturnsError()
if err != nil {
    // handle error
}
Alternatively, a new keyword could be introduced:

go
err := funcThatReturnsError() catch {
    // handle error
}
### 4. Drawbacks

- **Backwards Compatibility:** Changing existing syntax could break backwards compatibility, requiring significant updates to existing codebases.
- **Learning Curve:** Developers would need to adapt to new syntax, potentially leading to confusion and resistance.
- **Consistency:** Ensuring the new syntax is consistent with existing language patterns and idioms could be challenging.

### 5. Alternatives

- **Status Quo:** Maintain the current 'if err != nil' syntax, focusing on improving error handling through libraries and tools rather than language changes.
- **Existing Patterns:** Adopt existing patterns from other languages, such as Rust's `Result` type or Swift's do-catch blocks, potentially through a more direct integration or inspiration.

### 6. Unresolved Questions

- **Specific Syntax:** What specific syntax changes would be most beneficial and palatable to the Go community?
- **Implementation Roadmap:** How would the proposed changes be implemented, and what is the estimated timeline?
- **Community Adoption:** How to ensure smooth transition and adoption of the new syntax across the Go community?

---

*RFC generated by Resurrection Bot 🧬*
