# RFC: [feat] upstream nix requirements to nixpkgs

## 1. Summary

This RFC proposes that Tauri maintains a Nix derivation in nixpkgs with dependencies matching the documentation. This will enable Tauri users to easily use Nix for building and packaging their applications.

## 2. Motivation

The Tauri team has seen an increasing number of users interested in using Nix for building and packaging their applications. However, the current process of setting up a Nix environment for Tauri is manual and error-prone. By maintaining a Nix derivation in nixpkgs, we can provide a standardized and easily-maintained way for users to use Nix with Tauri.

The Nix ecosystem has continued to mature, with improvements to nixpkgs and the Nix language. The Tauri team has also grown, and there is now more resources to dedicate to platform-specific issues. Additionally, the Rust ecosystem has become more stable, making it easier to maintain complex derivations.

## 3. Detailed Design

To implement this feature, we will create a Nix derivation that mirrors the dependencies and configuration outlined in the Tauri documentation. This derivation will be maintained in nixpkgs, ensuring that users can easily use Tauri with Nix without manual configuration.

The derivation will include a shellHook to set up the necessary environment variables, such as WEBKIT_DISABLE_COMPOSITING_MODE. We will also work with the NixOS community to ensure that the derivation is properly tested and validated.

The derivation will be structured as follows:

*   The derivation will depend on the necessary dependencies, such as Rust, Node.js, and other required packages.
*   The derivation will include a shellHook to set up the necessary environment variables.
*   The derivation will be tested and validated by the NixOS community.

## 4. Drawbacks

One potential drawback of this proposal is the additional maintenance burden on the Tauri team. Maintaining a Nix derivation in nixpkgs will require regular updates to ensure that the derivation remains compatible with the latest versions of the dependencies.

Another potential drawback is the complexity of the Nix derivation. The derivation will require a good understanding of Nix and the Nix language, which may be a barrier to some contributors.

## 5. Alternatives

One alternative to this proposal is to provide a script or a set of instructions for users to set up a Nix environment for Tauri manually. However, this approach is error-prone and may not provide a consistent experience across different platforms.

Another alternative is to use a different package manager, such as Cargo or npm, to manage dependencies. However, Nix provides a unique set of benefits, including reproducibility and isolation, that make it an attractive choice for building and packaging applications.

## 6. Unresolved Questions

One unresolved question is how to handle platform-specific dependencies and configuration. Tauri supports multiple platforms, including Windows, macOS, and Linux, each with its own set of dependencies and configuration requirements.

Another unresolved question is how to ensure that the Nix derivation remains up-to-date with the latest versions of the dependencies. This will require regular maintenance and updates to the derivation.

Finally, there is a question of how to handle issues and bugs related to the Nix derivation. This will require coordination with the NixOS community and the Tauri team to ensure that issues are properly addressed and resolved.

---

*RFC generated by Resurrection Bot 🧬*
