# RFC: offer to create missing bucket and lock_table for s3 backend

## Summary
This RFC proposes a new feature for Terraform to automatically create a missing S3 bucket and lock table for the S3 backend.

## Motivation
The current implementation of the S3 backend in Terraform requires users to manually create an S3 bucket and lock table before initializing the backend. This can be inconvenient and prone to errors. By automating this process, we can improve the user experience and reduce the risk of errors.

## Detailed Design
The proposed feature would involve creating a new `terraform init` command that checks if the specified S3 bucket and lock table exist. If they do not exist, it would prompt the user to create them. The command would use the AWS SDK for Go v2 to create the S3 bucket with versioning enabled and the lock table with the correct configuration.

## Drawbacks
One potential drawback of this feature is that it may introduce additional complexity to the Terraform codebase. Additionally, there may be security implications to consider when automating the creation of S3 buckets and lock tables.

## Alternatives
One alternative to this feature would be to continue requiring users to manually create S3 buckets and lock tables. However, this approach has several drawbacks, including the potential for errors and the inconvenience to users.

## Unresolved Questions
One unresolved question is how to handle errors and exceptions that may occur during the creation of S3 buckets and lock tables. Another question is how to integrate this feature with the Terraform CDK to allow for more customizable and flexible configurations.

---

*RFC generated by Resurrection Bot 🧬*
