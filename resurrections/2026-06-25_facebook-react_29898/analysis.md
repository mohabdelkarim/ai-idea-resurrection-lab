# Analysis: [React 19] Disabling prerendering siblings of suspended components breaking common pattern

> The React 19 change breaks a common data fetching pattern, causing performance issues.

**Why it will work now:** React's concurrent rendering advancements and new Cache API enable a solution.

---

## Why It Died

The issue was abandoned due to lack of consensus on a solution. The change in React 19 caused performance problems for a common pattern of data fetching near where the data is used, turning parallel requests into waterfall requests. The discussion around potential fixes, such as making the change opt-in, did not lead to a resolution.

## Why 2026 Changes Everything

With the advancements in React's concurrent rendering and the introduction of new features like React's Cache API, it's now possible to implement a solution that balances performance and the common pattern of data fetching. The recent improvements in React's documentation and community support also make it more feasible to address this issue.

## Modern Architecture

To address this issue, we can introduce a new prop on the Suspense component, 'prerenderSibling', which defaults to 'true' to maintain the current behavior. This prop can be set to 'false' to opt-out of prerendering siblings of suspended components, allowing for more control over data fetching patterns. We can also provide a new hook, 'usePrerendering', to give developers more fine-grained control over prerendering behavior. Additionally, we can update the React documentation to provide clear guidelines on how to use these new features to optimize performance.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 9/10 |
| ⏱️ Effort Estimate | ~120 hours |
| 🏷️ Tech Tags | react, typescript, performance |
| 💀 Year Abandoned | 2025 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
