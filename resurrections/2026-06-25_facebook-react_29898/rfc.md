# RFC: [React 19] Disabling prerendering siblings of suspended components breaking common pattern

# Summary

The React 19 change that disables prerendering siblings of suspended components breaks a common data fetching pattern, causing performance issues. This RFC proposes a solution to address this issue by introducing a new prop on the Suspense component and a new hook to give developers more control over prerendering behavior.




# Motivation

In React 19, a change was made to disable prerendering siblings of suspended components. While this change was intended to improve performance, it ended up breaking a common data fetching pattern. This pattern involves fetching data near where it is used, which can lead to parallel requests. However, with the change in React 19, these parallel requests turned into waterfall requests, causing performance issues.




# Detailed Design

To address this issue, we propose the following:

*   Introduce a new prop on the Suspense component, 'prerenderSibling', which defaults to 'true' to maintain the current behavior. This prop can be set to 'false' to opt-out of prerendering siblings of suspended components.
*   Provide a new hook, 'usePrerendering', to give developers more fine-grained control over prerendering behavior. This hook can be used to determine whether a component is being prerendered or not.
*   Update the React documentation to provide clear guidelines on how to use these new features to optimize performance.




# Drawbacks

The proposed solution has the following drawbacks:

*   It adds a new prop to the Suspense component, which may cause confusion for developers who are not familiar with it.
*   It introduces a new hook, which may require additional learning and documentation.
*   It may not completely eliminate the performance issues caused by the React 19 change, but it provides more control over prerendering behavior.




# Alternatives

The following alternatives were considered:

*   Making the change opt-in: This approach was considered, but it did not gain consensus.
*   Disabling prerendering siblings of suspended components by default: This approach was considered, but it may cause performance issues for some applications.




# Unresolved Questions

The following questions remain unresolved:

*   How will developers use the new prop and hook to optimize performance?
*   What are the performance implications of using the new prop and hook?
*   How will the new features be documented and communicated to developers?

---

*RFC generated by Resurrection Bot 🧬*
