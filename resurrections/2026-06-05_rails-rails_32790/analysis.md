# Analysis: Folder with active storage

> Organize ActiveStorage files into folders by model.

**Why it will work now:** Rails 7.0+ ActiveStorage improvements enable folder organization.

---

## Why It Died

The issue was abandoned likely due to the lack of a clear solution at the time. ActiveStorage was a relatively new feature in Rails, and organizing files into folders was not a straightforward task. The community may not have had the bandwidth to implement this feature.

## Why 2026 Changes Everything

With the advancements in Rails and the ecosystem, specifically with the introduction of ActiveStorage's `service` and `blob` concepts, organizing files into folders has become more feasible. The `ActiveStorage::Service` module now provides more flexibility in handling file uploads and storage.

## Modern Architecture

To implement this feature, we can leverage ActiveStorage's `blob` and `service` concepts to create a hierarchical folder structure. We can create a new ` Folder` model that will hold the files and use a `service` to handle the file uploads. The folder structure can be achieved by using a combination of the model's id and a custom folder name.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 5/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | rails, activestorage, ruby |
| 💀 Year Abandoned | 2021 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
