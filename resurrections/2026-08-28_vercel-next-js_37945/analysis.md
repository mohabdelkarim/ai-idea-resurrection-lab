# Analysis: ISR fails to serve 404 pages once the page gets deleted if experimental isr memory cache size is set to 0

> ISR memory cache disabled leads to stale pages instead of 404 after deletion.

**Why it will work now:** Next.js 13 introduced Edge Runtime and improved ISR caching, enabling reliable 404 handling.

---

## Why It Died

The bug required deep changes to Next.js' ISR memory cache handling, which conflicted with the team's focus on edge runtime features. At the time, the ISR implementation lacked a clean hook to invalidate cached entries when memory cache size was zero. Limited testing resources and the low frequency of this specific configuration meant the issue received little community attention. Additionally, the core team prioritized upcoming major releases over fixing niche cache edge‑cases.

## Why 2026 Changes Everything

Next.js 13 introduced a unified Edge Runtime with built‑in cache invalidation APIs, allowing precise control over ISR entries. The new `unstable_cache` and `revalidateTag` utilities provide deterministic eviction without relying on memory size heuristics. TypeScript 5.2's improved type inference simplifies integration of these APIs into the ISR pipeline. Finally, the Vercel platform now offers serverless functions with automatic stale‑while‑revalidate support, reducing the need for custom memory cache logic.

## Modern Architecture

Implement a dedicated ISR cache manager class that abstracts storage backends (memory, edge, disk). The manager exposes `get`, `set`, and `invalidate` methods, leveraging Next.js 13's `unstable_cache` for edge storage and falling back to an in‑memory LRU when `isrMemoryCacheSize` > 0. When `isrMemoryCacheSize` is zero, the manager bypasses the in‑memory layer entirely and relies on `revalidateTag` to purge stale entries on each request. Integration points include the request handler middleware, which calls `cacheManager.get` and, on a miss, triggers a revalidation fetch. Errors during revalidation propagate to a 404 response if the source file no longer exists. Unit tests mock both edge and memory backends, ensuring deterministic behavior across configurations.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~32 hours |
| 🏷️ Tech Tags | typescript |
| 💀 Year Abandoned | 2022 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | No |

---

*Analysis generated by Resurrection Bot 🧬*
