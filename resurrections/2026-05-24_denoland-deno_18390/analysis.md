# Analysis: Feature Request: Importing a `js` file could fall back to `ts` file in a `ts` file

> Implement a fallback import resolution strategy in Deno for TypeScript files to import '.js' files that may also have '.ts'.

**Why it will work now:** This feature increases compatibility with other TypeScript tooling like ESbuild and TSC, enhancing Deno's usability in TypeScript projects.

---

## Why It Died

The feature request was declined due to potential complexity and ambiguity in resolving import paths.

## Why 2026 Changes Everything

With advancements in Deno's plugin system and the introduction of more sophisticated import resolution mechanisms, this feature can now be revisited with clearer implementation paths.

## Modern Architecture

The feature can be implemented by introducing a new ImportMap resolver that checks for the existence of files with '.js' and '.ts' extensions. This resolver can be built using Deno's existing plugin architecture and could utilize a strategy pattern to fallback between '.js' and '.ts' files.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 7/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | typescript, import resolution, deno |
| 💀 Year Abandoned | 2023 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
