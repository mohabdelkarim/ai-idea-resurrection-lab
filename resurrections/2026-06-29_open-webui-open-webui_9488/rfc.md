# RFC: Thinking doesn't showing for Deepseek R1 via API (external connection)

### 1. Summary

Open WebUI currently does not display thinking steps for Deepseek R1 API connections. This RFC proposes a solution to implement the display of thinking steps for Deepseek R1 via API connections.

The proposed solution involves modifying the API connection logic in Open WebUI to handle the streaming of thinking steps from Deepseek R1. This will be achieved by creating a new API client in TypeScript that can handle the specific streaming protocol used by Deepseek R1. The UI components will be updated to display the thinking steps in real-time.

### 2. Motivation

The motivation behind this proposal is to enhance the user experience of Open WebUI by providing more transparency into the thinking process of Deepseek R1. By displaying the thinking steps, users can better understand how the model arrives at its conclusions, which can lead to increased trust and reliability in the model.

Furthermore, the ability to display thinking steps can also facilitate debugging and testing of the model, allowing developers to identify potential issues and improve the model's performance.

### 3. Detailed Design

The proposed solution involves the following steps:

1. Research the Deepseek R1 API documentation to understand the streaming protocol used for thinking steps.
2. Create a new API client in TypeScript that can handle the streaming protocol used by Deepseek R1.
3. Modify the existing API connection logic in Open WebUI to use the new API client.
4. Update the UI components to display the thinking steps in real-time.
5. Implement error handling and logging mechanisms to ensure that any issues with the thinking step streaming are properly handled.

The new API client will be designed to handle the specific streaming protocol used by Deepseek R1, which may involve using WebSockets or WebRTC for real-time communication. The UI components will be updated to display the thinking steps in a clear and concise manner, potentially using a dedicated panel or section.

### 4. Drawbacks

The proposed solution has the following drawbacks:

1. Additional complexity: The implementation of the new API client and the modification of the existing API connection logic may add complexity to the codebase.
2. Performance impact: The streaming of thinking steps may have a performance impact on the application, particularly if the thinking steps are lengthy or complex.
3. Error handling: The implementation of error handling and logging mechanisms may add additional complexity to the codebase.

### 5. Alternatives

The following alternatives were considered:

1. Using an existing library or framework to handle the streaming protocol used by Deepseek R1.
2. Implementing a polling mechanism to retrieve thinking steps at regular intervals.
3. Providing an option for users to toggle the display of thinking steps.

However, the proposed solution was deemed the most suitable approach as it provides a seamless and real-time experience for users.

### 6. Unresolved Questions

The following questions remain unresolved:

1. What is the specific streaming protocol used by Deepseek R1 for thinking steps?
2. How will the thinking steps be formatted and structured for display in the UI?
3. What are the performance implications of streaming thinking steps, and how will they be mitigated?

These questions will need to be addressed during the implementation phase of the proposal.

---

*RFC generated by Resurrection Bot 🧬*
