# Proof of Concept: Name of task containing block is not printed to stdout

**Language:** python
**Estimated run time:** < 5 minutes

## Prerequisites

- Python 3.12+, pip install requirements

## How to Run

```bash
pip install -r requirements.txt
python main.py
```

## What This Demonstrates

Ansible blocks do not print the name of the task containing the block to stdout.
