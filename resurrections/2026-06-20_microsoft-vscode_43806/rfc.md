# RFC: Allow custom setting/control of workbench tab titles

## 1. Summary

This RFC proposes the introduction of a new setting that allows users to customize workbench tab titles in VSCode. The goal is to provide users with more control over the tab titles, enabling better navigation and organization of their workbench.

## 2. Motivation

The current workbench tab titles are automatically generated based on the file name and its location. However, some users have expressed a desire to customize the tab titles to better suit their needs. For example, users might want to include the project name, Git branch, or other relevant information in the tab title. By providing a customizable tab title feature, we can improve the overall user experience and make VSCode more flexible and adaptable to different workflows.

## 3. Detailed Design

To implement this feature, we propose introducing a new setting, `workbench.tab.titleFormat`, which will allow users to specify a custom format string or a function that takes the file name and returns a formatted string. The setting will be designed to be extensible, allowing users to leverage existing variables and functions, such as `${fileName}` and `${gitBranch}`, to construct their custom tab titles.

The setting will be stored in the VSCode settings file, and users will be able to configure it through the Settings UI or by editing the settings file directly. We will also provide a set of predefined format options and examples to help users get started.

## 4. Drawbacks

One potential drawback of this feature is that it may add complexity to the VSCode settings and user interface. If not implemented carefully, the feature could lead to user confusion or information overload. Additionally, the feature may have performance implications if not optimized properly.

## 5. Alternatives

One alternative to this feature is to use existing extensions or workarounds, such as using the `tabSize` setting or adjusting file naming conventions. However, these solutions have limitations and may not provide the same level of customization and flexibility as the proposed feature.

## 6. Unresolved Questions

Some questions that remain to be addressed include:

* What is the optimal syntax and structure for the `workbench.tab.titleFormat` setting?
* How will we handle conflicts between user-defined tab titles and existing tab title formats?
* What are the performance implications of this feature, and how will we optimize it for large-scale use cases?
* How will we ensure that the feature is accessible and usable for users with different skill levels and workflows?

By addressing these questions and concerns, we can ensure that the proposed feature meets the needs of VSCode users and provides a valuable addition to the editor's functionality.

---

*RFC generated by Resurrection Bot 🧬*
