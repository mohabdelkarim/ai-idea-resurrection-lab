# RFC: Have Fragments support dangerouslySetInnerHTML

# Summary

This RFC proposes to add support for `dangerouslySetInnerHTML` to React Fragments. This will allow developers to inject HTML content into a Fragment without the need for a wrapping DOM element.

# Motivation

React Fragments are a useful feature for grouping children without adding extra nodes to the DOM. However, they currently do not support `dangerouslySetInnerHTML`, which is a prop that allows developers to inject HTML content into a component. This limitation makes it difficult to use Fragments with HTML content that needs to be rendered directly.

The motivation behind this proposal is to enable developers to use Fragments with `dangerouslySetInnerHTML` to inject HTML content without the need for a wrapping DOM element. This will provide more flexibility and make it easier to work with HTML content in React applications.

# Detailed Design

The proposed design involves extending the Fragment component to accept a `dangerouslySetInnerHTML` prop. This prop will be used to inject HTML content into the Fragment.

The implementation will involve updating the Fragment's type definition to include the `dangerouslySetInnerHTML` prop. The necessary logic will also be implemented to handle HTML injection.

The API will be similar to the existing `dangerouslySetInnerHTML` API used in React components. The usage will be as follows:

<Fragment dangerouslySetInnerHTML={{ __html: htmlString }} />
This design aligns with React's existing API patterns and takes advantage of modern React features.

# Drawbacks

One of the potential drawbacks of this proposal is the security risk associated with using `dangerouslySetInnerHTML`. If not used carefully, this prop can make an application vulnerable to XSS attacks.

However, React has existing measures in place to mitigate these risks, such as the requirement to use the `__html` key with the `dangerouslySetInnerHTML` prop. Additionally, the React team can provide guidelines and best practices for using this prop safely.

# Alternatives

One alternative to this proposal is to use a wrapper element, such as a `div`, to render the HTML content. However, this approach can be limiting in certain scenarios where a wrapping element is not desired.

Another alternative is to use a library or utility function to handle HTML injection. However, this approach can add extra complexity and may not be as straightforward as adding a prop to the Fragment component.

# Unresolved Questions

One unresolved question is how to handle the case where a Fragment has both children and a `dangerouslySetInnerHTML` prop. One possible approach is to throw an error in this case, as it is not clear which content should be rendered.

Another unresolved question is how to handle the case where the HTML content injected into a Fragment contains React components. This may require additional implementation to handle the rendering of these components correctly.

Overall, this proposal provides a clear and straightforward way to add support for `dangerouslySetInnerHTML` to React Fragments. With proper implementation and guidelines, this feature can provide more flexibility and make it easier to work with HTML content in React applications.

---

*RFC generated by Resurrection Bot 🧬*
