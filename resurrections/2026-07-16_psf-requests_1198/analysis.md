# Analysis: max-retries-exceeded exceptions are confusing

> Improve max-retries-exceeded exceptions for clarity and usability.

**Why it will work now:** Tenacity library offers flexible retry mechanisms, improving error handling.

---

## Why It Died

The issue was not prioritized due to lack of clear demand and potential complexity of changing the existing behavior. The error message was seen as informative, albeit not immediately clear to some users. The urllib3 library, which handles the underlying retries, did not provide a straightforward way to customize retries at the time.

## Why 2026 Changes Everything

Modern Python libraries and practices around error handling and retries have evolved. Libraries like tenacity offer more flexible and user-friendly retry mechanisms. Additionally, Python's exception handling best practices have matured, emphasizing clearer error messages and more informative exception chaining.

## Modern Architecture

To address this issue, we could introduce a new exception type, e.g., `requests.exceptions.MaxRetriesExceededWarning`, that provides clearer guidance on what went wrong and potentially how to fix it. The `requests` library could also be updated to use a more configurable retry mechanism, such as tenacity, allowing users to specify their retry policies more explicitly. This would involve updating the `ConnectionError` handling in `requests/adapters.py` to catch and re-raise the exception with more context.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 5/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | requests, python, urllib3 |
| 💀 Year Abandoned | 2018 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
