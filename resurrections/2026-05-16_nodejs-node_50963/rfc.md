# RFC: enable corepack by default

# 1. Summary

Enable corepack by default for Node.js projects that specify a 'packageManager' in their package.json. This change aims to streamline package management and improve compatibility with modern package managers like pnpm.

## Overview

Corepack is a package manager that allows developers to use different package managers (e.g., npm, yarn, pnpm) without installing them globally. The goal of this RFC is to enable corepack by default for projects that specify a 'packageManager' in their package.json. This change will simplify package management and improve compatibility with modern package managers.

## Benefits

- Simplifies package management for Node.js projects
- Improves compatibility with modern package managers
- Reduces the need for manual configuration

## High-Level Plan

1. Integrate corepack with the 'packageManager' field in package.json
2. Introduce a new configuration option or flag to toggle corepack on or off
3. Update Node.js core modules to support corepack

# 2. Motivation

The Node.js ecosystem is evolving, and package management has become more complex. With the introduction of 'packageManager' in package.json, it's time to enable corepack by default. This change will benefit developers by simplifying package management and improving compatibility with modern package managers.

## Problems with the Current Status Quo

- Manual configuration of corepack is required
- Inconsistent behavior across different package managers
- Limited support for modern package managers

## Goals

- Enable corepack by default for projects that specify a 'packageManager'
- Simplify package management
- Improve compatibility with modern package managers

# 3. Detailed Design

## Integration with 'packageManager'

- When a project specifies a 'packageManager' in their package.json, corepack will be enabled by default
- The 'packageManager' field will be used to determine which package manager to use

## Configuration Option

- Introduce a new configuration option or flag to toggle corepack on or off
- The option will be called '--corepack' or 'corepack.enabled'

## Updates to Node.js Core Modules

- Update the 'package.json' parser to support the 'packageManager' field
- Update the package manager core modules to support corepack

## Example

{
  "packageManager": "pnpm@7.0.0"
}
# 4. Drawbacks

## Potential Backwards Compatibility Issues

- Existing workflows that rely on manual configuration of corepack may be disrupted
- Projects that do not specify a 'packageManager' may experience unexpected behavior

## Mitigation Strategies

- Provide clear documentation and warnings about the change
- Offer a configuration option to opt-out of corepack

## Performance Impact

- The performance impact of enabling corepack by default is expected to be minimal
- Benchmarks will be run to ensure that the change does not significantly affect performance

# 5. Alternatives

## Using the 'engine' Field

- The 'engine' field in package.json could be used to specify the package manager
- However, this approach has limitations and does not provide the same level of flexibility as 'packageManager'

## Manual Configuration

- Developers could continue to manually configure corepack
- However, this approach is cumbersome and prone to errors

## Comparison of Alternatives

| Alternative | Advantages | Disadvantages |
| --- | --- | --- |
| Using 'engine' field | Easy to implement | Limited flexibility |
| Manual configuration | Fine-grained control | Cumbersome and error-prone |

# 6. Unresolved Questions

## What are the Performance Implications?

- Benchmarks will be run to determine the performance impact of enabling corepack by default

## How will Existing Workflows be Affected?

- Existing workflows that rely on manual configuration of corepack may need to be updated
- Clear documentation and warnings will be provided to mitigate disruptions

## What are the Security Implications?

- The security implications of enabling corepack by default are expected to be minimal
- Security reviews will be conducted to ensure that the change does not introduce new vulnerabilities

---

*RFC generated by Resurrection Bot 🧬*
