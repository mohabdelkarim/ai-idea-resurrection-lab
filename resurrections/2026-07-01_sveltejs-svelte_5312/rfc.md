# RFC: $$slots with conditional/reactive content?

## Summary

This RFC proposes enhancing Svelte's $$slots feature to support conditional and reactive content. The current implementation of $$slots only works with static content, and this proposal aims to extend its functionality to handle dynamic slot content.

## Motivation

The $$slots feature in Svelte allows components to access their slots, but it currently only works with static content. As Svelte is increasingly used in complex applications, the need for more robust slot handling has grown. Recent advancements in Svelte's compiler, such as the introduction of Svelte 5's new reactivity system, make it feasible to revisit this issue.

## Detailed Design

To implement this feature, we would need to modify Svelte's compiler to track the reactivity of slot content. This could involve introducing a new node type in the abstract syntax tree (AST) to represent conditional slot content. We would also need to update the $$slots object to reflect the reactivity of the slot content. This could be achieved by using Svelte's existing reactivity system to create a reactive $$slots object that updates when the slot content changes.

The proposed implementation would involve the following steps:

1. Update the AST to include a new node type for conditional slot content.
2. Modify the compiler to track the reactivity of slot content.
3. Update the $$slots object to reflect the reactivity of the slot content.
4. Provide a new API for accessing reactive slot content.

## Drawbacks

The main drawback of this proposal is the complexity of implementing reactive slot detection with conditional content in Svelte's compiler. This would require significant changes to the compiler and may have performance implications.

## Alternatives

An alternative approach would be to provide a manual API for developers to handle slot content reactivity. However, this would require additional boilerplate code and may not provide the same level of seamless integration as a built-in solution.

## Unresolved Questions

The following questions remain unresolved:

* How to handle cases where slot content is updated dynamically, but not through Svelte's reactivity system?
* How to optimize the performance impact of tracking reactivity in slot content?
* How to ensure backwards compatibility with existing codebases that rely on the current $$slots implementation?

---

*RFC generated by Resurrection Bot 🧬*
