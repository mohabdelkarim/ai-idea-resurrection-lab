# Analysis: maybe an optimizable point for zadd operation

> Optimize Redis zadd operation for small score deltas by avoiding unnecessary node re-insertion.

**Why it will work now:** Redis ziplist and dict improvements make optimizing zadd for small score deltas feasible now.

---

## Why It Died

The issue was abandoned likely due to the complexity of modifying the existing zadd operation to optimize for small score deltas, and potentially the lack of immediate performance benefits for most use cases.

## Why 2026 Changes Everything

With modern advancements in Redis's internal data structures and the increasing demand for high-performance updates in sorted sets, revisiting this optimization could be feasible. Specifically, improvements in Redis's ziplist and dict implementations could make this optimization more straightforward.

## Modern Architecture

To optimize the zadd operation for small score deltas, we could modify the zsetAdd function to first check if the new score is within the range of the current node's neighbors. If so, only update the score without re-inserting the node. This could involve adding a new function to check the node's position in the skiplist and updating the score accordingly. The Redis codebase has evolved since 2018, with improvements in data structures and testing, making this a more manageable task.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 2/10 |
| ⏱️ Effort Estimate | ~40 hours |
| 🏷️ Tech Tags | redis, sorted-sets, skiplist |
| 💀 Year Abandoned | 2018 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
