# Analysis: Exclude files or directories from editor.formatOnSave

> VSCode should allow users to exclude specific files or directories from the editor.formatOnSave operation.

**Why it will work now:** The addition of native glob pattern support in VSCode settings since 2024 enables selective formatOnSave exclusions.

---

## Why It Died

The proposal required changes to the core formatting pipeline, which lacked a flexible exclusion API at the time. Maintaining backward compatibility with existing formatters added complexity that the team could not prioritize. Limited engineering bandwidth and competing priorities for core features caused the issue to stall.

## Why 2026 Changes Everything

VSCode now includes native glob pattern handling in settings.json, introduced in the 2024 release. The extension API has been extended with WorkspaceConfiguration.update supporting array patterns. Additionally, the editor.formatOnSave implementation was refactored to allow per-resource toggles, making selective exclusion feasible.

## Modern Architecture

Introduce a new setting editor.formatOnSaveExclude accepting an array of glob patterns. Extend the FormattingEditProvider to query this setting before invoking formatters. Modify the SaveParticipant to filter documents against the exclude list using the built-in glob matcher. Update the SettingsRegistry schema to validate the new array type. Ensure the feature integrates with existing language-specific formatOnSave toggles and respects workspace trust. Add unit tests covering pattern matching and interaction with existing formatOnSave configurations.

---

## Resurrection Score

| Metric | Value |
|--------|-------|
| 💥 Impact Score | 6/10 |
| ⏱️ Effort Estimate | ~32 hours |
| 🏷️ Tech Tags | vscode, typescript |
| 💀 Year Abandoned | 2025 |
| 🔬 Has PoC | Yes |
| 📋 Has RFC | Yes |

---

*Analysis generated by Resurrection Bot 🧬*
