# RFC: Move away from electron

## 1. Summary

This RFC proposes migrating the Visual Studio Code (VSCode) application from Electron to Tauri. The goal is to improve performance, security, and scalability by leveraging Tauri's Rust-based core. This migration will involve rearchitecting the application, adapting the extension ecosystem, and integrating a new core API layer.

## 2. Motivation

VSCode currently uses Electron, which has served well but presents challenges in terms of performance and security. Electron's architecture, based on Chromium and Node.js, results in a larger memory footprint and potential security vulnerabilities. Tauri, built on Rust, offers a more secure and lightweight alternative. The benefits of migrating to Tauri include:

- Improved performance: Reduced memory usage and faster startup times.
- Enhanced security: Rust's memory safety features and Tauri's secure architecture reduce the attack surface.
- Better scalability: A more modular architecture facilitates easier maintenance and updates.

## 3. Detailed Design

The migration to Tauri involves several key steps:

1. **Core API Layer**: Develop a new core API layer using Rust, which will serve as the backbone of the application. This layer will handle critical functionalities such as file system interactions, process management, and security features.

2. **Integration with Existing Frontend**: Integrate the new Rust-based core API layer with the existing TypeScript-based VSCode frontend. This will involve creating adapters or wrappers to ensure compatibility between the Rust core and the TypeScript frontend.

3. **Extension Ecosystem Adaptation**: Adapt the extension ecosystem to work with the new Tauri-based architecture. This may involve changes to the extension development APIs and ensuring that existing extensions are compatible with the new core.

4. **Microservices Architecture**: Apply a microservices architecture pattern to decouple the core editor from the extension ecosystem. This will enhance scalability and facilitate easier updates and maintenance.

## 4. Drawbacks

The migration to Tauri presents several challenges:

- **Complexity**: The migration involves significant architectural changes, which can be complex and time-consuming.
- **Compatibility**: Ensuring compatibility with existing extensions and plugins may require substantial effort.
- **Resource Intensive**: The migration will require considerable resources, including developer time and computational power.

## 5. Alternatives

- **Electron Alternatives**: Explore other Electron alternatives, such as Neutralino or Quasar, to assess if they offer similar or better benefits than Tauri.
- **Incremental Improvements**: Focus on incremental improvements to the current Electron-based architecture, such as performance optimizations and security enhancements, rather than a complete migration.

## 6. Unresolved Questions

- **Timeline**: What is the estimated timeline for the migration process?
- **Resource Allocation**: What resources (developers, computational power) will be required for the migration?
- **Extension Compatibility**: How will extension compatibility be ensured, and what support will be provided for extension developers?

---

*RFC generated by Resurrection Bot 🧬*
